# Reference Formats and Resolution

One file per section, each self-contained. Read only the files for your job, and when you need more than one, Read them all in one parallel batch.

| Section | File |
|---|---|
| Request conventions — which tools take a MODEL reference, list parameters, the two key spellings, rejected keys | `references/reference-formats/request-conventions.md` |
| MCP Response Envelope | `references/reference-formats/response-envelope.md` |
| Node Info Envelope | `references/reference-formats/node-info-envelope.md` |
