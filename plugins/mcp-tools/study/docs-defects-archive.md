# Archived documentation defects (skill-script automation study)

Moved out of [`docs-defects.md`](docs-defects.md) on 2026-09-22, with D43–D47 added on 2026-09-24 and D49, D51, D52, D54, D55, D56, D57, D58, D59, D60, D61, D62 (`b727f965586a`), and D69 (`edbc0a14a9ec`) on 2026-09-25. These entries are **no longer to be handled**: they are fixed or closed, out of scope, rejected, or not locally actionable.

The live open list is [`docs-defects.md`](docs-defects.md).

### Why these left the live list

- **Fixed / closed** — D1, D3–D13, D14b, D15–D17, D19–D30, D31, D33–D39, D43–D47, D49, D51, D52, D54, D55, D57, D58, D59, D60, D61, D62, D69, A1, H2.8. (D31's live status was stale: round-4 remedy P6 shipped 2026-09-18. D43–D47 were archived 2026-09-24; D49, D51, D52, D54, D55, D57, D58, D59, D60, D61, D62 (`b727f965586a`), and D69 (`edbc0a14a9ec`) on 2026-09-25. D49, D51, D52, D54, D55, D58, D59, D60, D61, D62, and D69 leave with re-measure still outstanding; the optional D51/D52 server hint stays in [`../docs/d51-d52-assignability-hints-plan.md`](../docs/d51-d52-assignability-hints-plan.md), not on the live list.)
- **Out of scope** — D2 (not a worker prompt; fixture-builder note only).
- **Rejected / removed / no further local work** — D14 (no top-level alias; decision 2), D18 and D18b (remaining `projectPath` cost is platform pre-dispatch routing, outside `mcp-tools`; filing is the user's decision), D32 (constrained `format` type blocked by the enum-decode trap; description tightening is not a fix), R7 (`includeChildRoleConcepts` removed after 14 unused runs), D56 (archived 2026-09-25: each cited repeated miss was one parallel batch, recovered in one turn; only the rejection wording was aligned with the shared near-miss list).

| # | Where | Defect | Evidence | Status |
|---|-------|--------|----------|--------|
| D1 | `mps-mcp-workflow/references/mcp-tools-index.md` | Study 1.2(4) claims the index omits `search_root_node_by_name`, `parse_java_and_insert`, `create_run_configuration`. | All three present as of commit fc28c2636dce (verified 2026-09-15). | already fixed |
| D2 | `test_scenarios/StateChartScenario.md` | Contains reporting requirements ("keep ongoing notes", "final report"), which study 6.2 forbids in worker prompts. | file header | not a worker prompt; use as fixture builder only |
| D3 | tool descriptions / `mps-node-editing` (file-path `childJson`) | On macOS the worker wrote its blueprint to `/tmp/...` and got `Input file path '/tmp/…' is not inside the system temp directory`. Neither the tool description nor the skill says the file must be under `java.io.tmpdir` (`$TMPDIR`, `/var/folders/...` on macOS), and the error does not name the accepted directory. | S1-opus-1 (first error result, ~step 40) | **fixed 2026-09-15** — error text names `$TMPDIR` / `java.io.tmpdir`; `json-format.md` and `troubleshooting.md` document `/tmp` vs `$TMPDIR`. Round 2: `/tmp` rejection absent. |
| D4 | `mps_mcp_get_concept_details` (and likely other name-resolving tools) | `unresolved[].suggestions` for a plain concept name are taken from the shared module repository, so with a same-named language in another open project the suggestions point into that other project (conceptReference with the foreign language UUID). Not a docs defect strictly — a tool-behaviour issue: suggestions should be scoped to the selected project or be labelled with their project. Note this entry names only the suggestion-scoping issue; round 3's defect table used the label `D18/D4` for missing `projectPath`, which is D18 alone — D4 is not an alias of it. | S1-sonnet-1, get_concept_details result containing `c:d10ad89a-…` (opus project) | **closed 2026-09-17** (original suggestion-scoping reading) — verified through the suite's own cross-project harness rather than a live two-project session, and pinned by tests in `ProjectResolutionCrossProjectTest` (committed `190aa53fa82c`). Both halves of the contract hold: a plain-name **miss** issued as project A yields no suggestion from project B's namespace (`get concept details suggestions never name a concept from a sibling project`), and a plain name existing **only** in project B does still resolve — the same read-only cross-project access the module/model tools document — but is **marked** rather than silently followed, carrying `containingProject` (name + `mpsProjectBaseDirectory`) and `editableFromCurrentProject:false` (`get concept details marks a sibling-project concept reached by plain name`). So the contract is not "never reached" but "never reported as if it belonged to the caller's project". **Fixture limitation, recorded honestly**: the suggestion ranker iterates `LanguageRegistry.allLanguages`, and a language created by `LanguageProducer` has no compiled runtime and is therefore absent from the registry — so project B's language is excluded for two reasons at once (unregistered *and* filtered). The suggestion test consequently guards the end-to-end **wiring** (that suggestions are scoped at all, and that no other path re-widens them) while the pre-existing predicate test, `name-suggestion candidates exclude concepts owned by a sibling project`, is what proves the **filter** (`ProjectMembershipCache.isFromAnotherOpenProject`: own-project concept kept, same-named sibling-project concept dropped, read-only library concept kept). Nothing stronger is reachable without a real make cycle in the harness. D4 was never an alias of D18, and is no longer carried as one. **Second reading, closed 2026-09-22**: round 6 found the same *word* ("leak") describing a different mechanism — `search_concepts` answering out of the whole language registry, so a four-term query came back dominated by `jetbrains.mps.transformation.test.outputLang` and friends. That is registry **noise**, not cross-project leakage: the sibling-project filter was already applied and working. It is addressed by the `scope` parameter added for D38, which defaults to the languages this project owns **or uses** — deliberately not owned-only, which would drop `jetbrains.mps.baseLanguage` in any project that merely uses it and make `ClassConcept` unfindable. Same fixture limitation as above, and for once it bites harder: this checkout *is* MPS-development, so every bundled language is a project module and `scope:"project"` is very nearly `scope:"all"` here. The narrowing is therefore pinned by a predicate test (`search scope predicate separates own languages from library and sibling ones`, which asserts the three-way split own / library / sibling that the scope helper depends on) rather than end-to-end; the integration tests guard the wiring — that the scope is applied, that `"all"` re-widens, that `scope`+`modelReference` is rejected, and that an empty narrow pass auto-widens with a warning |
| D5 | `mps_mcp_print_node` JSON output / `analysis-tools.md` | An enum property holding the enumeration's *default* literal is printed as `"value": ""` (MPS stores nothing for the default). The evaluator initially read 13 recipes as missing `difficulty`; the plain-text projection shows `easy`. The output schema doc should state that an empty enum value means "default literal" (or the printer should emit the default's name with a flag). | S3-opus-1 evaluation; fixture recipe Crepes shows the same | **fixed 2026-09-17** — enum default is printed with the literal name and `"isDefault": true`; `enumerationDefault` is on `get_concept_details`; `analysis-tools.md` ("Default property values are invisible in dumps") states that an omitted/`""` value means the default, not missing. |
| D6 | `mps_mcp_create_module` result / `mps-mcp-workflow` | Both S1 workers tried to create `mcp.study.recipes.structure` after `create_module` had already created the aspect models (`No suitable model root found … to create model`). The result should list the models it created; the skill should say a new language comes with structure/editor/… models. | S1-opus-1, S1-sonnet-1 first error envelope | **fixed 2026-09-15** — `create_module` returns `data.models` (`[{name, reference, aspect}]`); tool description and `mcp-tools-index.md` say a new language already has its aspect models. Round 2: the "No suitable model root" retry is gone. |
| D7 | `mps_mcp_check_root_node_problems` (model reference) | Workers re-check every root after a clean model-level result (S3-opus-1: 50 calls). The envelope should state coverage, e.g. `rootsChecked: 48`, so the model-level answer is visibly exhaustive. | S3-opus-1 steps ~35–95 | **fixed 2026-09-15** — model-scope check envelope carries `details.scope:"model"` and `details.rootsChecked`; `perRoot=true` lists every root. Documented in `analysis-tools.md` and the tool description. |
| D8 | `mps_mcp_get_project_structure`, `mps_mcp_print_node` | Always return a temp-file path, even for tiny payloads, forcing a second call to read it (32/23/25/4 envelopes per run). Inline small results; add `fields`/`summary` options for the large ones. | all runs; chains #1, #6, #9 | **fixed 2026-09-15** — `maxInlineBytes` (default 20000) inlines small `print_node` / `get_project_structure` / `get_concept_details` / `query_nodes` / `check_root_node_problems` payloads; temp-file path only above the threshold (`analysis-tools.md`). |
| D9 | `mps_mcp_print_node` `format` / `analysis-tools.md` | Allowed literals (`JSON`, `HTML`, `PLAIN TEXT`) are not listed; the space-containing literal is unguessable ("text" rejected). | S1-sonnet-1:110-111 | **fixed 2026-09-15** — allowed literals `JSON` / `HTML` / `PLAIN TEXT` (space) are in the tool description, `troubleshooting.md`, and `analysis-tools.md`. Round 2: `"text"` rejection gone. |
| D10 | `mps_mcp_update_node` `childJson` vs `insert_root_node_from_json` | Top-level array accepted by the insert tool but rejected by `update_node` (`Expected JsonObject but was JsonArray`); asymmetry undocumented, forced 3 single-child calls. | S1-sonnet-1:129-132 | **fixed 2026-09-15** — documented as an explicit contract: insert tools accept a top-level array, `update_node` `childJson` is a single object (`Expected JsonObject but was JsonArray` in `troubleshooting.md` / `json-format.md` / `bulk-creation.md`). |
| D11 | file-path `childJson` error text | "Write the JSON to a temp file (e.g. via File.createTempFile)" is a Java hint given to a shell agent; should name `$TMPDIR`. Worker wrote itself a memory note about it. | S1-opus-1:16-18, :173 | **fixed 2026-09-15** (with D3) — error names `$TMPDIR`; Java `File.createTempFile` hint dropped. |
| D12 | `get_project_structure includeNodes` dump | Properties holding their default value are omitted from the dump, so verification scripts report false "missing" values (`KeyError: 'difficulty'`). Same root cause as D5. | S3-opus-1:28-33, :97 | **fixed 2026-09-17** (same root cause as D5) — dumps omit default-valued properties; `analysis-tools.md` and `mps_dump.py` treat absence as the default (`enumerationDefault` / `isDefault`). Round 2: the `KeyError: 'difficulty'` script traceback is gone. |
| D13 | `alter_structure UPDATE_CONCEPT_REFERENCE`, `mps-aspect-structure-concepts` | `multiple` is read and honoured for children but never read for references (hard-coded single-valued, cardinality `_0__1`/`_1`); unknown keys in `parameters` are dropped silently, so `multiple:true` on a reference returns `ok:true` and quietly yields a 0..1 link. No shipped skill states that MPS reference links are always single-valued, nor names the smart-reference wrapper idiom for `[0..n]` references. | round 2: S1-sonnet-1:16-57 (~41 tool calls to rediscover, incl. a scratch-concept experiment at `:42`); source `JetBrainsMPSLanguageStructureMcpToolset.kt:291-296` and ~1204; `structure-operation-api.md:204` vs `:209-220` | **fixed 2026-09-17** — N2 is in place (verified 2026-09-17): `REFERENCES_ARE_SINGLE_VALUED` (`McpToolInputSchemas.kt:47`) rejects `multiple:true` on a reference in both the blueprint parsers and `UPDATE_CONCEPT_REFERENCE`, and M6 extends the same reject-the-ignored-key policy to the `…Ref`/`…Reference` alias family (D14b). N1 is in place too: `mps-aspect-structure-concepts/SKILL.md:48`, `:55-60` ("Reference links are always single-valued", plus the "Multi-valued references" smart-reference wrapper recipe), restated at `structure-operation-api.md:234` and `concept-details.md:60`. Both halves therefore **fixed**; the status line had simply not been refreshed. The row's broader observation that *unknown blob keys are dropped silently* still holds outside the alias family |
| D14 | `mps_mcp_get_concept_details` | `conceptReference` — the parameter name `print_node`/`scaffold_editor` use — is not recognised here. Round 2's reading ("the error names neither the offending key nor the correct one") no longer holds: the message names the plural `conceptRefs`/`languageRefs` and calls out the singular spellings explicitly (`JetBrainsMPSLanguageMcpToolset.kt:104-108`), and `search_concepts` likewise answers `"searchTexts is required: …"` (`:240`). Round 3's cost was therefore **not** an unnamed key — the agent fetched the schema anyway. | round 2: S1-sonnet-1:95; round 3: S1-sonnet-1:56-58 (`query` vs `searchTexts`) | partly fixed 2026-09-17 — M5b: both rejections now end with the literal edit to make (`"Retry with conceptRefs set to the value you passed as conceptReference…"`, `"Retry with searchTexts set to the value you passed as query/q/text."`), and the `conceptRefs`/`languageRefs` rejection carries an `INVALID_REQUEST` code it previously omitted. No alias parameter was added (decision 2: it would grow the per-turn schema). **Value unproven**: in both observed incidents the message already named the correct key and the model fetched the schema anyway, so only a round-4 re-measure can say whether a retry line changes that. The blob-key alias half (M6) landed 2026-09-17 as well — see below |
| D14b | every `parameters`-blob key whose idea has two spellings (hotspot 7, the family that produced a new member in each of rounds 2 and 3: `conceptReference`/`conceptRefs`, then `query`/`searchTexts`) | The surface carries one idea under two conventions — blob keys use the short `…Ref` suffix, top-level parameters the long `…Reference` — and an unknown **blob** key is dropped silently, so a caller who carries the other spelling gets a "Parameter 'X' is missing" for a value it did pass. Inventory re-derived against the source 2026-09-17: 8 pairs over 30 read sites. | round 2: S1-sonnet-1:95; round 3: S1-sonnet-1:56-58; source: 30 `paramString` sites across `JetBrainsMPSLanguageStructureMcpToolset.kt`, `JetBrainsMPSNodeMcpToolset.kt`, `AbstractNodeOps.kt:1383` | fixed 2026-09-17 — M6. Blob keys accept **both** spellings via one reader, `JsonObject.paramString(BlobKey)` (`McpToolInputSchemas.kt`), applied to `conceptRef`/`conceptReference`, `superConceptRef`/`superConceptReference`, `structureModelRef`/`structureModelReference`, `enumerationRef`/`enumerationReference`, `nodeReference`/`nodeRef`, `childNodeRef`/`childNodeReference`, `newParentRef`/`newParentReference`, `modelReference`/`modelRef`. Free per decision 3: a blob key never appears in the published MCP schema. Two spellings of one key present at once is **rejected** naming the winner — the same "reject the ignored key" policy as `REFERENCES_ARE_SINGLE_VALUED` (D13/N2) — because a caller who sent both cannot tell which won; a field-level `null` still counts as absent under either spelling. **No top-level alias was added** (decision 2, H10: a parameter three rounds never exercised is a permanent tax on the ~97,052-token per-turn floor); instead the remaining top-level "required parameter missing" rejections were brought into the M5b voice — `update_node`'s six (`nodeReference`, `childRole`, `childJson`, `childNodeRef`, `properties`, `references`), which gained the `INVALID_REQUEST` code they omitted, the retry line, and the near-miss spelling this tool does *not* accept; and `search_root_node_by_name` no longer answers a blank `names` with `ok:true` + `[]` (a silent drop, reachable because `JsonOrText` maps `null` to the empty string) but with the same named rejection `search_concepts` gives for `searchTexts`. Drift surface: the 8 pairs are pinned as literals in `McpToolInputSchemasTest` (spellings, canonical-first, one stem per pair, either spelling resolves, conflict rejected, null-not-a-conflict, explicit-null detection) plus alias/conflict/message integration tests in the structure and node suites. Docs: `reference-formats.md` "Two spellings for one reference key" (with the no-top-level-alias rule), `structure-operation-api.md`. **Same caveat as D14**: in both observed incidents the message already named the right key and the model fetched the schema anyway — the alias half removes a real dead end, the message half is unproven until round 4. **Second half closed 2026-09-21**: the silent drop itself is gone. Every `parameters` blob is now checked against a per-operation accepted-key set before dispatch (`ParameterKeys` / `rejectUnknownParameterKeys` in `McpToolInputSchemas.kt`), so an unrecognised key is answered with `INVALID_REQUEST` naming it, the nearest accepted spelling (`suggestParameterName`, moved into `common` alongside the check) and the whole accepted set — it is never read past. Two consequences worth recording: `dryRun` is now accepted only by `CREATE_CONCEPTS` and `CREATE_ENUM`, the two `alter_structure` operations that honour it (elsewhere it was read off the blob and the model mutated anyway — the same silent no-op `parse_java_and_insert` already rejects), and a field-level JSON `null` counts as absent for the check, because it carries no value to drop. `alter_nodes MAKE` keeps its own richer rejection (`MAKE_INPUT_INVALID` plus an `expectedParameters` schema map). Pinned by unit tests in `McpToolInputSchemasTest` and integration tests in the node and structure suites |
| D15 | `mps_mcp_update_node SET/CHILD` | Docs say `childJson = null` deletes the child, but the string `"null"` gives "Input is neither a valid JSON object/array nor an existing file path: 'null'" without stating how to express the null. | round 2: S1-sonnet-1:154 | fixed 2026-09-17 (row updated 2026-09-21 — the fix landed with the round-3 batch and the table was never revised). `readJsonOrFile` intercepts the 4-character string before the file-path branch and answers `"Input is the string 'null', not a JSON object/array or a file path. Where a null is meaningful for this parameter, express it by omitting the parameter entirely or sending an unquoted JSON null."` (`AbstractOps.kt`). Deliberately generic: the same helper serves required parameters (`conceptsJson`, `valuesJson`) where a null means nothing. Pinned by `JetBrainsMPSNodeMcpToolsetIntegrationTest.set-node-child rejects the string null and says how to express the null`, which also asserts the rejected call neither replaced nor deleted the child, and stated in the `childJson` description and in `mps-node-editing/SKILL.md` |
| D16 | shipped catalog vs installed copies | The 2026-09 treatment updated `plugins/mcp-tools/resources/**` only; the checkout's own `.agents/skills/` and `.claude/skills/` (42 differing entries) and every already-initialised project still serve pre-treatment docs — no `scripts/`, no `rootsChecked`, no jump tables. Refreshing the study's golden project was a precondition for round 2. | `diff -rq` of resources vs both trees, 2026-09-16 | fixed 2026-09-17 — catalog propagated to the checkout (N6, verified by `diff -rq`); the per-run install (D19) stops it recurring in the study |
| D17 | `study/scripts/analyze_runs.py` | A run that delegates to a subagent emits one `result` event per session; the analyzer kept the last, reporting the subagent's numbers (S1-sonnet-1: 13 turns / 92 s instead of 115 / 952). | round 2: two `result` events in S1-sonnet-1-worker.jsonl | fixed 2026-09-16 (keep the event with the most turns; baseline re-run is byte-identical) |
| D18 | `mps_mcp_list_open_projects` (round 2 labelled this "D4, restated"; it is not — see D4) | The discovery call itself refuses to run without `projectPath`, even with exactly one project open. An omitted `projectPath` is rejected *before dispatch*, and an ancestor path (repo root when the MPS project is a subdirectory) is rejected the same way. `initialize_project_for_agents` previously contradicted the other guides by saying not to pass it. | round 2: S3-sonnet-1:10, S3-sonnet-2:6; round 3: S1-sonnet-1:11-13 (2 of 5 error envelopes, 2 wasted turns); observer hit it twice | **partly fixed 2026-09-17 / 2026-09-18** — M3a reconciled the contradictory instructions: one unconditional `projectPath` Critical Directive in `mps-mcp-workflow/SKILL.md` (also `AGENTS_template.md` and the two repo-root guides). **2026-09-18 (`ee21bc97`):** the empty first-call handshake was replaced by a CWD probe — if `projectPath` is unknown, pass the session working directory and scrape the rejection list only if that is unmatched. That is the best-effort MPS-side mitigation: it removes the omitted-`projectPath` cost when CWD is at or inside the open project, and otherwise recovers in one turn. Remaining cost is the ancestor/subdirectory case and the gated discovery tool (`list_open_projects` still cannot run without a matching path). The pre-dispatch rejection itself is platform routing and cannot be fixed here. **M3b write-up restored** from `ee21bc97` after it was deleted in `ff94d507`: [`docs/projectpath-pre-dispatch-rejection-upstream.md`](../docs/projectpath-pre-dispatch-rejection-upstream.md). **Nothing has been filed** — no issue, no PR, no external contact; filing is the user's decision. Round-2 remedy N3b (`HOTSPOT_REPORT_round2.md:225`) is discharged by that document |
| D19 | `study/fixtures/*.tar.gz` + `study/scripts/run_worker.sh` | The harness half of D16: a fixture tarball carried `.claude/skills`, `.agents/skills`, `AGENTS.md` and `CLAUDE.md`, so every run of every later round measured the catalog as it was when the tarball was built, while `promptSha256`/`inventorySha256` pinned only the scenario and the tool surface — a catalog change was invisible in the evidence. | round 2 precondition (manual refresh); `FIXTURE_SKILLS_REFRESH_PLAN.md` | fixed 2026-09-17 — fixtures exclude the doc surface; `run_worker.sh` installs the live catalog per run via `scripts/install_skills.py` and records `skillsSha256` |
| D20 | every top-level MCP parameter documented as accepting a JSON array (13 tools' worth, 6 toolsets) | Re-scoped 2026-09-17: the round-3 reading ("a fully-qualified concept name in `conceptRefs` raises an unhandled `Index -1 out of bounds for length 0`") named the wrong cause on both counts. **The FQN form resolves fine** — plain concept, interface concept and a concept in an uncompiled project language all return `ok:true`, and a hollow descriptor by FQN returns a clean `NOT_FOUND`. What crashed is the **wire shape**: passing a real JSON array to any `String`-typed top-level parameter dies in the *platform's* argument binding, before the tool body runs. `CallableBridge.call` resolves `serializerOrNull(parameter.type)` per Kotlin parameter and hands it the request's raw `JsonElement`; for a `String` parameter that is `StringSerializer`, which takes the tree-*list* decode path and pops an empty tag stack (`TaggedDecoder.popTag`). **No MPS frame is on the stack**, so the remedy plan's M5a step 2 (a `try/catch` around the per-ref loop) could not have helped, and its step 3 (fix FQN resolution) was unnecessary. The round-3 evaluator sent `{"conceptRefs":["…Recipe","…Step"]}` (a real array) and crashed while the same run's worker sent the array-as-*string* `"[\"…\"]"` and succeeded. Every affected parameter's own `@McpDescription` already promised array support, several with array literals, so this reached real users in 15 places, not just the study. | round 3: S1-sonnet-1 evaluation, 2026-09-17; the same `SEVERE`-logged `McpSessionHandler` stack in `log/idea.1.log:57049`, `log/idea.2.log` and `log/idea.6.log`, months apart | fixed 2026-09-17 — the whole documented-array family is now declared `JsonOrText` (`JsonOrText.kt`), a serializer whose descriptor is a `PrimitiveKind.STRING` so the published schema stays byte-identical to `String`'s `{"type":"string"}` (measured: `JsonElement` would have cost +58 chars per parameter on every turn *and* published `"type":"object"`, telling a client not to send a string) while decoding a real array, the array-as-string, a plain value, an object, a number or `null`. The 15 parameters: `get_concept_details.conceptRefs`/`.languageRefs`, `search_concepts.searchTexts`, `search_root_node_by_name.names`/`.models`/`.modules`, `model_dependency.targetModels`, `create_module.facets`, `scaffold_editor.includeComponents`/`.includeProperties`/`.includeReferences`/`.includeChildren`, `insert_root_node_from_json.json`, `update_root_node_from_json.json`, `insert_console_command_from_json.json`. Pinned by `McpJsonOrTextWireShapeTest` (schema shape, optionality, both wire shapes, malformed values ⇒ `ok:false`, and a guard that fails if a new documented-array parameter is left `String`) |
| D20b | `parameters` of `query_nodes` / `alter_nodes` / `query_structure` / `alter_structure` / `parse_java_and_insert`; `update_node.childJson`; `update_module_facet.settingsJson` | Same platform defect as D20, one wire shape over: these parameters document a JSON **object** rather than an array, so a client that sends `{"conceptRef":"…"}` instead of `"{\"conceptRef\":\"…\"}"` hits the identical `Index -1 out of bounds for length 0`. Not covered by the D20 fix, whose approved scope was the array-documented family. | derived from the D20 root cause, 2026-09-17 (not observed in a run) | fixed 2026-09-21 — all seven are now `JsonOrText` and unwrap `.text` at the call site, so the published schema stays `{"type":"string"}` (the D20 measurement carries over unchanged) while a real JSON object decodes. In-process callers and tests keep a `String` entry point: the four operation tools already had enum-typed `(operation, parameters: String)` overloads, and `parse_java_and_insert` gained the same kind of non-`@McpTool` overload. `update_node.childJson` and `update_module_facet.settingsJson` are `JsonOrText?`, so the nullable wrapper still intercepts an unquoted JSON null before the serializer and the documented "null deletes the child" meaning is unchanged (D15). `McpJsonOrTextWireShapeTest` gained the object-shaped counterpart — one case per *consumer* of the decoded text (the operation dispatchers' `Gson().fromJson`, `readJsonOrFile`, `jsonToMemento`), the lower-bound count raised 15 → 22, and a second guard, `JSON-payload parameters are all JsonOrText, never String`, which fails on any `@McpTool` parameter named `parameters` or ending in `json` that is left `String`. Docs: `reference-formats.md`, `mcp-tools-index.md`, `structure-operation-api.md`, `parse-java-tips.md`, `mps-node-editing/SKILL.md` (whose "`childJson` takes the string form only" line was the one statement the change falsified) |
| D21 | `study/scenarios/S1/done_criteria.md` criterion 2 | Names `query_structure` / `search_concepts` for "list the concepts of a language", which neither tool can do: `query_structure` has no list-concepts operation and `search_concepts` hard-requires `searchTexts`. The workable route is `get_project_structure(startingPoint=<structure model>)` + `get_concept_details(languageRefs=…)`. | round 3: S1-sonnet-1 evaluation | fixed 2026-09-17 — M9 rewrote criterion 2 around that route and, since an enumeration has no lookup by qualified name (D22), routes the enum check through the roots dump: `print_node(deep=true)` or `GET_ENUMERATION_LITERALS(enumerationRef=<node ref>)`. The worker prompt is untouched (`promptSha256`) |
| D22 | `mps_mcp_get_concept_details` | An enumeration cannot be resolved by qualified name: `mcp.study.recipes.structure.Difficulty` returns `NOT_FOUND` and suggests the unrelated `…structure.Recipe`. The declared default is reachable only through an owning concept's `properties[].enumerationDefault` — which is exactly the lookup the D5 "is this enum value missing?" pitfall forces on every reader. Re-scoped 2026-09-17: the root cause is structural, so `get_concept_details` can **never** resolve an enumeration — concept resolution requires `root.concept.isSubConceptOf(concept_AbstractConceptDeclaration)` (`AbstractOps.kt:2673`, `:2691`) and `EnumerationDeclaration` is not one. The route the error should name, `query_structure GET_ENUMERATION_LITERALS`, resolves `enumerationRef` through `resolveNodeReferencePreferringProject` (`JetBrainsMPSLanguageStructureMcpToolset.kt:1102`). **Correction 2 of the remedy plan overstated this half** and is superseded: `resolveNodeReference` has a by-name fallback (it runs because `SNodePointer.deserialize` throws `IncorrectNodeIdFormatException` for any string without a `/`), so `<model>.<EnumName>` and a bare `<EnumName>` already resolved. Only the **language-qualified** `<language>.<EnumName>` form did not, because the fallback compared `model.name.longName == modelName` exactly and never tried `"$modelName.structure"`. So it is not true that a qualified enumeration name had no working lookup anywhere; the substantive gap was the missing routing (below) plus that third name shape. | round 3: S3-sonnet-1 evaluation, 2026-09-17 | fixed 2026-09-17 — M5c, both halves. (1) `GET_ENUMERATION_LITERALS`' `enumerationRef` now resolves a **qualified name** as well as a node reference: the node-reference attempt falls back to `resolveStructureDeclarationPreferringProject`, a project-first structure-model scan accepting `<model>.<Name>`, `<language>.<Name>` and the bare `<Name>` (the same name forms as concept resolution, minus the `AbstractConceptDeclaration` filter). Blob-level, so no schema cost (decision 3). Its unresolved message now names `'enumerationRef'` and both accepted forms, keeping it distinguishable from the second input form (`nodeReference` + `propertyName`), which is unchanged; the "not an EnumerationDeclaration" rejection is kept. (2) `get_concept_details` detects a ref that names a non-concept structure declaration and answers with what it *is* plus the working call — `"'X' is an EnumerationDeclaration, not a concept … Retry with mps_mcp_query_structure, operation GET_ENUMERATION_LITERALS, parameters {\"enumerationRef\":\"X\"}"` — in the error message, in the partial-success `warnings`, and as `declaredAs`/`route` on the `details.unresolved` entry, whose `suggestions` are now deliberately **empty**: a similarly-named concept from the same structure model is a dead end, not a near miss (round 3: `…structure.Difficulty` → `…structure.Recipe`). A non-enumeration declaration (e.g. a data type) is routed to `print_node` with its node reference instead. Docs: `concept-details.md` gained an "An enumeration is not a concept" section, `structure-operation-api.md` documents the qualified-name form, `node-editing-rules.md` names it at the point the enum-literal lookup is prescribed |
| D23 | `mps_mcp_print_node` (JSON format) + `analysis-tools.md` | Re-worded 2026-09-17: the round-3 premise ("no depth parameter") is **wrong** — `deep=true` already inlines all descendants (`JetBrainsMPSNodeMcpToolset.kt:815`, dispatched `:822`/`:829`, documented at `analysis-tools.md:57`), and `format:"PLAIN TEXT"` renders the whole root in one call (~10x cheaper for verification). The actual defect is routing: the docs did not name `deep=true` / `PLAIN TEXT` as *the* verification path, so the evaluator walked the children at one call per step while JSON stayed at its shallow default. | round 3: S3-sonnet-1 evaluation | fixed 2026-09-17 — M4 added the "Verifying a whole root costs one call" paragraph to `analysis-tools.md` plus cross-references from the tool-inventory bullet and the `check_root_node_problems` section. Docs only; no server change needed |
| D24 | `mps_mcp_print_node` PLAIN TEXT + `analysis-tools.md` | An EMPTY `0..n` role renders as `see also: << ... >>`, which a naive reader parses as a one-element list. The empty-collection placeholder is undocumented. | round 3: S3-sonnet-1 evaluation | fixed 2026-09-17 — M4 documented it next to the verification recipe in `analysis-tools.md`: an editor placeholder means the role is *empty*, and counts must come from the JSON form, never from the projection |
| H2.8 | `mps_mcp_query_nodes` `FIND_INSTANCES` (a hotspot, not a docs defect — recorded here because this file, not the report, carries the remedy statuses) | Counting instances cost one call per concept and serialised every node found only for the nodes to be reduced to integers afterwards. `S3-sonnet-1:23-27`: five consecutive `FIND_INSTANCES` calls (`Recipe`, `Cookbook`, `Step`, `RecipeRef`, `Ingredient`) purely for root counts; three exceeded `maxInlineBytes` and returned temp-file paths — **all three of round 3's temp-file envelopes come from this one chain** — followed by a `mps_dump.py` shell-out to turn the dumps into integers. Determinism 1.0. | round 3: `HOTSPOT_REPORT_round3.md` §2.8 | fixed 2026-09-17 — M1, two additive `parameters` keys (no schema cost, decision 3). `conceptRefs` takes one reference or an array and scans **once** for the union; `detail:"count"` answers `[{concept, conceptReference, count}]`, one row per requested concept in input order including `count:0`, and builds no `nodeInfoJson` at all (that serialisation, not the round trips, is what overflowed three of the five calls). `conceptRefs` is **not** a third `BlobKey` spelling of `conceptRef`/`conceptReference` — it is a different key with a different arity — so sending it together with either singular spelling is rejected naming the survivor, the same reject-the-ignored-key policy as D13/D14b; `detail:"count"` with `sampleOnly:true` is rejected as a contradiction. Counting correctness is the risk this remedy carries, so: a node is tallied on **first sight only** (both the facade and the fallback walk can report one node once per concept it matches), the index-coverage gate in `findInstancesWithFallback` became **per concept** so a batched query answers each concept exactly as a single-concept query would, and rows **overlap by construction** under `exact:false` (a subconcept instance counts for every requested superconcept, so the rows do not sum to a distinct-node total) — stated in the tool description and the docs, and pinned by tests for each failure mode plus a cross-check that a row equals the node count of the same query. The zero-risk docs route stays and is now cross-referenced honestly rather than replaced: `get_project_structure(includeRootNodes=true)` + `mps_dump.py count` needs no server call beyond a dump you may already hold, but is roots-only and ignores `propertyFilter`/`exact`/`scope`. `query_structure FIND_INSTANCES` (the unadvertised compat dispatch) serves both new keys. Docs: `analysis-tools.md` (FIND_INSTANCES section + a `detail:"count"` subsection), `mcp-tools-index.md`, `finding-things.md`, the `query_nodes` `@McpTool` description. **Correction to the remedy plan**: its implementation note that "the fallback walk does not honour `exact`" (quoting `AbstractNodeOps.kt:1349-1352`, `:1417-1422`) is wrong in the direction it claims — `InstanceLookup` asks `FastNodeFinder` with `includeInherited=false` and, unlike the facade's `InstancesSearchType`, does not expand the query set with descendants, so the fallback reports *only* exact instances. The per-concept filter is therefore a no-op on both paths today rather than load-bearing on one; it is kept (generalised over the requested set) because neither path documents that guarantee, and the misleading comment is corrected in place. The pre-existing consequence — a non-exact query served by the fallback misses subconcept instances — is unchanged by M1 and applies identically to `detail:"nodes"`, which is why the invariant M1 promises is "a count equals the node count of the same query", not "a count equals the true number of instances" |
| D25 | temp-file envelopes | The temp file named by `data` contains a COMPLETE `{"ok":…,"data":…}` envelope rather than the bare payload, so a caller that splices the file in as `data` gets `data.data`. Undocumented; cost one debugging round trip in the observer's own CLI. Scoped 2026-09-17: `analysis-tools.md:55` already stated the envelope shape for `print_node` and `:60` that the JSON mutation tools accept either form — only the generic "Read tools inline small results" paragraph at `:7` was silent. | round 3: observer, `get_project_structure` with 44 roots | fixed 2026-09-17 — M7 generalised the envelope shape to all read tools at `analysis-tools.md:7`. The payload stays an envelope: `saveToTempFile` writes `okJson(json)` (`AbstractOps.kt:3246-3247`) for every tool, so emitting the bare payload would change what every existing consumer parses for no measured gain |
| R7 | `mps_mcp_get_concept_details` `includeChildRoleConcepts` (a shipped remedy that never paid, not a docs defect — recorded here because this file, not the report, carries the remedy statuses) | R7 was the round-1 S-tier remedy for hotspot 6 (discovery refinement): `includeChildRoleConcepts:true` returned the one-hop child/reference role targets under `relatedConcepts` in the same call. It was called **0 times in 14 runs across all three rounds**, while its parameter and its sentence in a ~2,400-character `@McpTool` description cost schema bytes on **every turn of every session** — the per-turn floor H10 measures at ~97,052 tokens. Round-2 N8 proposed dropping it; round 3 is the third confirmation. | rounds 1–3: `HOTSPOT_REPORT.md:106` (the remedy), `HOTSPOT_REPORT_round2.md:83`, `:230` (N8, "currently dead weight"), `HOTSPOT_REPORT_round3.md:129`, `:195`, `:245` (M8) | **removed 2026-09-17** — M8. Deletion, not deprecation, on explicit user approval: deprecating keeps the schema cost and therefore forfeits the entire benefit, and the evidence for "no caller exists" is the study's 14 runs plus the parameter's absence from every bundled script (`SkillScriptsDriftTest` is unaffected) and every skill example. Sites removed: the parameter and its `@McpDescription`, both call-site threadings (the registered `JsonOrText` overload and the internal `List<String>` one), the `relatedConcepts` payload branch, the `roleTargetConcepts` helper (whose only caller was that branch), the sentence inside the `@McpTool` description, the `includeChildRoleConcepts` integration test, and the comment that enumerated the parameter among the tool's arguments. Hotspot 6 — the chain R7 was built for — is **not** left unanswered: it is served by a route that already existed and was already documented, and the removal is signposted with it in all four places the parameter used to be named (the tool description and `concept-details.md` / `analysis-tools.md` / `mcp-tools-index.md`, × three trees): **one `get_concept_details(languageRefs=…)` call returns every concept of the language**, which is strictly cheaper than any per-concept refinement chain and needs no follow-up when the next role target turns out to live in the same language. R7's plausible failure mode is worth keeping in view for future S-tier remedies: a parameter that needs a documentation fix to be discovered, and whose job a simpler existing call already does, is not worth its schema |
| D26 | `mps_mcp_parse_java_and_insert` + `mps-aspect-behavior/SKILL.md:35`, `mps-baselanguage/references/parse-java-tips.md` | `featureKind:"METHOD"` with a `contextNodeRef` that resolves to a `ConceptBehavior` root throws `jetbrains.mps.lang.smodel…NodeCastException: Can't cast node … ConceptBehavior to … baseLanguage.structure.Classifier` (`JavaParser.java:118` via `JetBrainsMPSJavaMcpToolset.kt:100`), reported to the caller as a bare `{"ok":false,"error":"Internal error while Parsing Java and inserting nodes","code":"INTERNAL_ERROR"}`. The server call log records the call as `ok:true`. Omitting `contextNodeRef` produces the correct explanation (`"'contextNodeRef' is required for featureKind 'METHOD' to provide the target Classifier"`), so the constraint is known and simply unchecked for the present-but-wrong-type case. Because a `ConceptBehavior` root can never be a `Classifier`, `featureKind:"METHOD"` is unusable for behavior methods — the exact call `mps-aspect-behavior/SKILL.md:35` recommends. | round 4: `S1-sonnet-1:92`, `log/idea.log` stack trace, reproduced twice post-round against the S3 project (both probes failed before insertion) | closed — round-4 remedy P1 shipped: `ConceptBehavior` contexts for `METHOD`/`CLASS_CONTENT` are now an accepted, converted path (parsed methods become `ConceptMethodDeclaration` in `method`); remaining wrong-type contexts get a structured `INVALID_REQUEST` error instead of the `NodeCastException` crash. The call log now reports true end-to-end `ok`/`threw`/`errorCode` via `McpCallOutcomes`, replacing the `ok:true` mis-logging. Rounds 1–4 `server_errors` were computed under the old `ok` semantics (crash-or-throw only) and are not comparable to post-fix runs. Implementation plan: `docs/round4-p1-d26-implementation-plan.md` |
| D27 | `mps_mcp_get_concept_details` | The singular `conceptRef` (no `-erence`) is rejected by the round-3 message that enumerates `conceptReference`/`languageReference`, so the text never names the key the caller actually sent. With parallel tool-calling this cost 8 rejections in one turn. | round 4: `S1-sonnet-1:133-140` | **fixed 2026-09-22** — P2. The empty-input rejection and `RETRY_WITH_CONCEPT_REFS` now name all four near-misses (`conceptRef`/`conceptReference`/`languageRef`/`languageReference`). None of them is a declared parameter, so the binder drops whichever was sent and every one arrives as "no input at all" — the message cannot echo the actual key and must therefore list all four. No alias parameter was added: `conceptRef` is the *canonical* blob key of the blob-taking tools (`PARAM_CONCEPT_REF`), so the asymmetry this defect is made of is by design and only the rejection can bridge it. Test: `get-concept-details rejects every singular near-miss with a copy-pasteable retry line`, which loops all four spellings and asserts the **quoted** singular (unquoted `conceptRef` is a substring of the `conceptRefs` the same message names, so it would assert nothing) |
| D28 | `mps_mcp_update_node` | Calling `ADD`/`CHILD` with `target`/`parentRef`/`role` (a natural guess, and the parameter names `parse_java_and_insert` itself uses for `insert`) yields the platform message "No argument is passed for required parameter 'kind'": it names 1 of 3 wrong keys and none of the correct ones (`kind`, `nodeReference`, `childRole`). 6 rejections in one turn, plus a `ToolSearch` schema re-fetch. | round 4: `S1-sonnet-1:148-154` | **fixed 2026-09-22** — P2, in two parts. (1) `operation` and `kind` default to `""`, so the call reaches the body instead of the binder; `missingUpdateNodeSelector` names whichever selector is actually blank, the valid pairs, the ADD CHILD keys and the near-misses. `operation` is resolved *before* `kind` is checked so `DELETE` still gets D36's recipe rather than a dead-end "kind is required". (2) ADD CHILD reports **every** missing key in one rejection — the incident sent `parentRef`/`role`/`target`, all three wrong at once, and one-key-per-call would have cost three round trips; a single missing key still takes the settled singular wording of `missingUpdateNodeParameter`. The optionality is itself asserted by `published schema keeps the selectors of update_node and alter_nodes optional`, so re-requiring a selector fails by name instead of surfacing as a bridge error in every sibling test. Two consequences of moving these rejections from pre-dispatch to in-tool were handled explicitly: (a) all of them now pass through `McpCallOutcomes.record`, since they return before `withMpsProject` and an unrecorded envelope logs as `ok:true` — which would have made round 7's `server_errors` improve as an artifact of the remedy being measured (D26). The correction cuts both ways and the cross-round comparison has to allow for it: D36's three observed `DELETE` guesses were **not** counted in rounds 1–6 `server_errors` (that path returned an unrecorded envelope), and an identical run would now count them. The same sweep recorded the batch `SET PROPERTY`/`SET REFERENCE` aggregates, which were logged as whatever their *last* row happened to be, and the D27/D32/`alter_nodes`+`query_nodes` unknown-operation rejections, which are those defects' entire error surface; (b) dropping out of the published `required` array removes the client's only structural "mandatory" signal, so all three descriptions now begin "Required." |
| D29 | `mps_mcp_alter_nodes` | `MAKE` with `moduleName`/`rebuild` at the top level yields "No argument is passed for required parameter 'parameters'" without saying that the module list belongs inside the `parameters` JSON string. | round 4: `S1-sonnet-1:163-165` | **fixed 2026-09-22** — P2. `parameters` defaults to `JsonOrText.EMPTY`, and the blank guard sits in the internal enum-typed overload rather than the `@McpTool` wrapper so neither entry point can reach `Gson.fromJson` with nothing to parse (blank text *and* a bare JSON null both decode to a null `JsonObject`. Only **MAKE** dereferenced it: `alterNodesParameterKeys` returns a key list for the other four operations, and `rejectUnknownParameterKeys` already answered a null blob with an actionable `ToolInputSchemaException`. Both inputs now answer identically: blank text hits the guard, and the literal 4-character text `"null"` — the one input that parses to a JSON null — falls through to the same `missingAlterNodesParameters`, so it too names the accepted keys instead of the bare `INVALID_JSON` an earlier revision of this fix gave it. The code is therefore that operation's own: `INVALID_REQUEST` for the four with a key list, `MAKE_INPUT_INVALID` for MAKE. The `"null"` literal path has no test — `JsonOrTextSerializer` maps a *wire* JSON null to `""`, so the bare-null test lands on the blank guard, not here). MAKE's message says its arguments belong inside the object, lists `MAKE_PARAMETER_KEYS`, and carries `details.expectedParameters`; the dropped-key hint never asserts what the caller actually sent — the binder cannot report which keys it dropped, and the message has to stay true for a caller who sent none — so it reads "a 'rebuild' put there never arrives" rather than claiming one was.  It also stops naming `moduleName` beside a real key as though both were keys: `moduleName` is not a key anywhere, and the message says so. Every other operation gets its own `alterNodesParameterKeys` list, never MAKE's. MAKE's rejection carries `MAKE_INPUT_INVALID`, not `INVALID_REQUEST`, so it stays in one errorCode class with every other MAKE input rejection (the contract recorded in D14b above). The rejection is passed through `McpCallOutcomes.record` because it returns before `withMpsProject` — see D26 |
| D30 | `mps_mcp_query_nodes` `FIND_INSTANCES` | `scope:"roots"` reads as "count root nodes only" but means "search within these root references" and requires a `roots` parameter; a caller after per-concept root counts picks it naturally and gets `Parameter 'roots' is missing for scope 'roots'`. | round 4: `S3-sonnet-1:25` (the round's only S3 error) | **fixed 2026-09-18** — Alternative 1: added orthogonal `rootsOnly: true` parameter across all scopes; missing `roots` under `scope: "roots"` now returns an actionable diagnostic directing caller to `rootsOnly: true`. |
| D31 | study harness (`run_worker.sh`, skill preflight) | User-level `~/.claude/agents/*.md` are visible to every headless worker. `mps-constraints-agent` was invoked 3× in round-4 S1; subagent MPS calls appear in the server call log under the parent's session id but **not** in the parent transcript, and the subagent's turns/tokens are absent from the `result` event — so `metrics.csv` understated that cell by 7 MCP calls. The skill's preflight asserts no user-level `mps-*` **skills** but says nothing about agents. Rounds 1 and 3 had 0 `Agent` calls, round 2 had 1. | round 4: server-vs-transcript cross-check (102 − 9 pre-dispatch = 93 expected, 100 logged) | **fixed 2026-09-18** — P6 landed (`HOTSPOT_REPORT_round4.md` verification): `study/scripts/check_user_agents.py` plus a mandatory `run_worker.sh` gate abort with exit 3 when `~/.claude/agents` contains an MPS-related definition (filename `*mps*` or body `mps_mcp`); `analyze_runs.py` warns and emits a surplus column when `server_calls > mps_calls − pre-dispatch rejections` and counts parent `Agent` tool-use events. The live-table status had not been refreshed. |
| D18b | platform `projectPath` routing — mid-run omission sub-family | Split out from D18/D4 after round 4. The **first-call** sub-family (agent does not yet know the project path; `list_open_projects` called with `{}` and rejected) is **fixed 2026-09-17** by the CWD-probe instruction in `ee21bc97f70d`: present in the baseline (S3-sonnet:7), round 2 (S3-sonnet-1:10, S3-sonnet-2:6) and round 3 (S3-sonnet-1:4), **absent in round 4** — `S3-sonnet-1:5` passes CWD as `projectPath` and is accepted first try. The **mid-run omission** sub-family is untouched and byte-identical in rounds 2, 3 and 4: `alter_structure CREATE_ENUM` at steps 11-12, after the same run already passed the correct path at steps 6 and 10 (100 of 102 calls carry it), worker comment "I forgot to include `projectPath`". Not addressable by wording — the heuristic's precondition ("path not yet known") does not hold. Both `parameters`-string tools show the same class of error: D29 is the mirror image on `alter_nodes MAKE`. | round 4: `S1-sonnet-1:11-13` vs `S3-sonnet-1:5`; four-round rejection census in `HOTSPOT_REPORT_round4.md` §2.12 | first-call half **fixed**; mid-run half **still open**, and P5(a) now understood to be unable to close it. P5(a) ("accept the key in either position") was implemented on 2026-09-21 — `projectPath` is tolerated inside every `parameters` blob, so the caller who repeats it there is not failed by the new strict-key check (D14b) — but that is the *only* reachable half. A call that omits the **top-level** `projectPath` is rejected by the platform before dispatch, so the tool body never runs and no in-blob value can be read, let alone honoured; see `docs/projectpath-pre-dispatch-rejection-upstream.md`. The measured mid-run incidents are all of that kind, so the saving P5(a) was estimated to produce is **not** realised and should not be claimed. What remains actionable is P5(b) (single-open-project fallback) and (c) (the upstream routing question), both outside `mcp-tools`. D29, cited here as the mirror image, **was** closed on 2026-09-22 by the one lever that is available inside `mcp-tools`: giving `parameters` a default so the call is dispatched and the body can answer. That lever does **not** transfer to `projectPath`: it is the platform's own top-level tool parameter (`McpToolInputSchemas.kt:70`), not a parameter of the MPS tool's Kotlin signature, so there is no signature here to give it a default in — see `docs/projectpath-pre-dispatch-rejection-upstream.md`. The D14b key check only covers the case where the caller does reach the tool and puts a wrong key *inside* the blob |
| D32 | `mps_mcp_print_node` | `format:"structural"` guessed for four consecutive nodes although D9 (fixed 2026-09-15) documents the three allowed literals `JSON` / `HTML` / `PLAIN TEXT`; the parameter is typed `string`, not a constrained enum, so nothing stops a plausible fourth guess. Proposed in round 5, logged now. | round 5: `S1-sonnet-1:27-30` | **mitigated 2026-09-22, mechanism still open** — P2 tightened the `@McpDescription` to the three literals (`JSON (default), HTML, PLAIN TEXT`) and `print_node rejects an unknown format quoting the rejected value` pins the rejection. That is **not** a fix and should not be counted as one: the allowed-set rejection already existed before this round and `mcp-tools-index.md:26` already documented the three literals more precisely than the tool did, so nothing in the change mechanically prevents a fifth guess — a free `String` parameter always admits one. The only real fix is a constrained type, which is blocked by the enum-decode trap documented in `AbstractOps.resolveOperationOrNull`'s KDoc (a Kotlin enum at the tool boundary throws `SerializationException` in the framework's pre-call decode, which is why every selector on this surface takes a `String`) — not by D20, which is the unrelated array-into-`String` crash — or would need a schema-level `enum` array that no annotation surface exposes today |
| D33 | `mps_mcp_check_root_node_problems`, `mps_mcp_print_node` | Model-scope addressing is asymmetric and undocumented. The checker rejects `modelReference` and requires the model reference string under `nodeReference` (round 5); it also rejects a plain model **name** (`Reference 'mcp.study.kitchen.samples' resolved to neither node nor model`) although `get_project_structure`'s `startingPoint` accepts one; and `print_node` rejects a model reference that the checker accepts. None of the three rejections names the accepted alternative. | round 5: `S1-sonnet-1:138`; round 6: `S5-sonnet-1:5` (plain name), `S5-sonnet-1:13` (`print_node` + model ref) | **fixed 2026-09-21** — checker accepts a qualified model name via `resolveModelPreferringProject`; `print_node` rejects a model with INVALID_REQUEST naming `mps_mcp_get_project_structure` (`startingPoint`, `includeNodes=true`); both document that the checker has no `modelReference` parameter |
| D34 | `mps_mcp_parse_java_and_insert` | The `package` declaration of the parsed source is **silently discarded**: the call returns `ok:true`, no `packageName` is set on the resulting `Classifier`, and the 2,450-byte tool description never uses the word "package". `Classifier.packageName` is what actually drives the generated Java package and output path (`BaseLanguageTextGen.getPackageName`, `TextGenAspectDescriptor.getPath_ClassConcept`), but the property appears nowhere in the generator/baseLanguage skills, and `mps-aspect-generator/SKILL.md`'s path convention names a `<java-package>` path segment without saying what sets it. The S6 worker wrote `package mcp.study.generated;` three times, saw `ok:true` each time, then tested `virtualPackage` (genuinely pinned 1:1 and irrelevant to Java output) and concluded — wrongly — that MPS cannot place generator output in a chosen package. | round 6: `S6-sonnet-1:20,21,24` + live read of `RecipeTemplate` (properties `isStatic`/`nestedName`/`name`/`resolveInfo`, no `packageName`) | **fixed** `e00c57599c2e` — `insertAsRoot` copies `parseResult.getPackage()` onto `Classifier.packageName` and warns when it diverges from the destination model name; pinned by `root CLASS insert copies a divergent package onto packageName and warns`. Archived as the silent-discard half of P8. Documenting `packageName` (the docs half of P8) is leftover and is no longer tracked here. |
| D35 | `mps_mcp_get_concept_details` | `languageRefs` cannot resolve a language module that exists in the open project but whose runtime is not deployed — both the qualified name and the canonical `l:<uuid>:<name>` form are rejected with `NOT_FOUND` — while `conceptRefs` with fully qualified concept names resolves concepts of that same language two steps later, before any `MAKE`. The rejection recommends `mps_mcp_search_concepts`, which returned 183 KB of unrelated whole-repository matches. Mechanism (likely: `languageRefs` enumerates via the deployed runtime, `conceptRefs` via the structure model) observed but not confirmed. | round 6: `S2-sonnet-1:10,11` rejected vs `:15` succeeding pre-`MAKE`; the `search_concepts` dead end at `:12-13` | **fixed 2026-09-21** — when the language module exists but `LanguageRegistry` has no runtime, the error/`warnings`/`details.unresolved.route` name MAKE (`rebuild=true`) and `conceptRefs` FQNs and no longer point at `search_concepts` (still used for genuine unknown names). Distinct from hollow-descriptor recovery. |
| D36 | `mps_mcp_update_node` | There is no `DELETE` operation — deletion is `SET` × `CHILD` with `childJson` omitted — but the rejection only says `Unknown operation 'DELETE'. Valid operations: ADD, SET.` and never says how to delete, so the same guess recurs (S5 made it twice in a row). | round 6: `S2-sonnet-1:36`, `S5-sonnet-1:26`, `S5-sonnet-1:27` | **fixed 2026-09-22** — P2, and the root cause was **not** the message. `mps-node-editing/SKILL.md` documented `DELETE` × `CHILD`/`PROPERTY`/`REFERENCE` as valid rows of the operation matrix, told the reader to "prefer `DELETE` × `CHILD`" over the null-`childJson` form, and named `propertyName`/`referenceRole` parameters that do not exist; `mps-mcp-workflow/references/mcp-tools-index.md` repeated all three phantom combinations. So the recurring guess was agents following the skill, and it would have recurred with any wording of the rejection. Both files are corrected in the blueprint and re-propagated to `.agents/skills` and `.claude/skills`; `unknownNodeUpdateOperation` replaces the generic `unknownOperation<NodeUpdateOperation>` call: it keeps that helper's enum-derived valid-operations list (now via `NodeUpdateOperation.entries`) and appends a deletion recipe chosen by `kind` — SET CHILD with `childJson` omitted for a child, SET PROPERTY / SET REFERENCE with an explicit null triplet value otherwise, and both forms when `kind` is blank or does not resolve. A recipe naming only the child form would be wrong advice for two of the three kinds. The recipe rides on *every* unresolvable operation, not only `DELETE` — the caller who reached for a verb this tool lacks is the one who needs to hear what it has. `test_scenarios/.../07_root_nodes_and_node_editing.md` step 07.28 pins this message and was updated twice — once for the appended recipe, then again when the recipe became `kind`-aware (it sends `kind:"CHILD"`, so it now pins the child form and notes that another kind names the triplet form). The corrected docs are deliberately narrower than the first attempt's: "deletion is a SET with the value omitted" is true only for `childJson`, since a shortened property/reference triplet is rejected (`triplet.size >= 3`) rather than read as a delete |
| D37 | `mps_mcp_print_node` / `mps_mcp_get_project_structure` | A **non-default** enum property value prints with its internal prefix — `"WtZI0zlAtJ/ML"`, `"5y90acY48Li/G"` — while default values print cleanly (`"PIECE"`, `isDefault:true`). D5 fixed only the default case; any comparison of a printed enum value against a literal name still mismatches. | round 6: fixture build (`Milk.unit`), independently reported by the S5 evaluator | **fixed 2026-09-21** — `print_node` / `get_project_structure` emit the declared literal name for non-default enum members |
| D38 | `mps_mcp_search_concepts`, `mps_mcp_get_project_structure` | No way to ask for less. `search_concepts` has neither `maxInlineBytes` nor projection and searches the whole repository (183 KB for four search terms, dominated by unrelated languages — D4's leak in a new place); `get_project_structure includeNodes` is all-or-nothing, so "what roots does this model have" costs the whole AST (40-87 KB, six times this round). R1's inlining threshold is untouched by either: every round-6 envelope was a genuinely large payload. `detail:"shape"` does not help when the request unit is a whole language (155 KB). | round 6: `S2-sonnet-1:12`; `get_project_structure → Read` ×7 at avg 65,671 chars (`analysis/chains.json`) | **fixed 2026-09-22** — round-6 remedy P9 implemented on both tools. `get_project_structure` gained `nodeDetail` (`"names"` = name/concept/reference per node, nothing else) and `nodeDepth` (bounds how far `includeNodes` inlines; at the cut-off a role lists `{name, reference}` children and is marked `childrenTruncated`); both default to today's output, so existing calls are byte-identical. `search_concepts` gained `maxInlineBytes` (it had none, unlike every sibling reader), `detail` **defaulting to the new compact `"summary"` record** — `doc`, `sourceNode`, `virtualFolder`, `superConcept` and the duplicated `superInterfaces`/`superInterfaceDetails` pair are projected away, `deprecated` is deliberately kept — a 50-entry cap on strict matches (they were uncapped; only the fallback heap was bounded) reported through `details.totalStrictMatches`/`details.truncated` plus a warning, and `scope` defaulting to `"project"`. **A free-form `fields` filter was considered and rejected**: hotspot 7 ("guessable-but-undocumented literals") is the dominant error family, and an arbitrary field vocabulary mis-spells into a silently wrong projection instead of a rejection; two named projections cost less schema and cannot. Not measured against a round-7 run — the saving is projected from the round-6 payloads, not observed. |
| D39 | `mps-project-management/references/examples-macos.md` (and the Linux/Windows siblings, which share the recipe) | The activation recipe passes the whole `java_command` field from `jcmd VM.command_line` as the main class: `cmd = [java, *filtered, "-classpath", classpath, java_command, project]`. That field is `"<main-class> [args]"`, so an MPS that was itself **started with a project path** reports `jetbrains.mps.Launcher /path/to/previous/project` and the reconstruction dies with `Could not find or load main class jetbrains.mps.Launcher .Users.vaclav…` / `ClassNotFoundException`. The fix is one token: `java_command.split()[0]`. This defect was dormant while MPS was only ever started from the IDEA run configuration without arguments; the study's own lifecycle automation (`mps_control.sh start <project>`, and the skill's own "MPS is not running — start with the project open") makes it reachable on every subsequent activation. | rehearsal 2026-09-22: `S10-sonnet-1:18` fails, `:19` succeeds after the worker changes it to `java_command_full.split()[0]` — one wasted turn, self-recovered | **fixed 2026-09-22** — from-sources recipes now take `java_command.split()[0]` (PowerShell: first split token) as the main class, matching `mps_control.sh` capture's `mainClass`. Blueprint `mps-project-management` updated (`examples-{macos,linux,windows}.md`, `open-via-cli.md`, `SKILL.md`) and re-propagated to `.agents/skills` and `.claude/skills`. |
| A1 | study asset — `plugins/mcp-tools/study/fixtures/README.md` | The `recipes` fixture is defined as the passing-S1 project **with the S1 sample Recipe and Cookbook roots deleted**. That rule exists for S3 alone ("exactly 40 Recipe roots plus the Cookbook"); S5 and S6 both need Recipe roots, and S6's criterion 2 ("one Java file per Recipe root") is vacuous without them. Round 6 introduced `recipes-full.tar.gz` (samples kept) and built `recipes-broken.tar.gz` on top of it. The README should scope the deletion to S3 and name `recipes-full` for S5/S6. | round 6 §0.1 | **fixed 2026-09-21** — README scopes sample-root deletion to S3 and names `recipes-full` for S5/S6 |
| D43 | tool descriptions / `mps-mcp-workflow` reference formats | Module and model parameters use six conventions across sibling tools — `conceptRef`, `conceptRefs`, `languageRefs`, `enumerationRef`, `modelReference`, `moduleName` — and `create_model`'s `moduleName` in fact accepts a module *reference*, the exact value the agent tried to pass as `moduleReference`. Five of nine retries in round 8 were this. Tools that validate in-body return a near-miss-aware message and cost one turn; `insert_root_node_from_json` (`modelReference`) and `create_model` (`moduleName`) declare the parameter required in the signature, so the platform answers with a generic "No argument is passed for required parameter 'x'" that never names the argument it dropped — `S2:75` then spent a `ToolSearch` to recover. | `S1-sonnet-1:103,109`; `S2-sonnet-1:68,75-77` (round 8) | **fixed** (2026-09-24) — **S**, whole surface rather than the two tools observed: the class was **27 of 37** published tools, every one answering a misspelled key with the binder's generic message. Every `@McpTool` parameter now has a Kotlin default (the published `required` array is empty everywhere); semantically required ones say "Required." and are rejected in the body by `AbstractOps.rejectMissingParameters` (`common/RequiredParameters.kt`): INVALID_REQUEST naming the key, a literal retry, and the near-miss spellings that never arrived, from one vocabulary (`RequiredParameterNearMisses`), with `details.missingParameters`. Blank-meaningful parameters are not listed (`create_root_node.name`, `create_module.name` for a generator); either-of pairs fold both values (`concept`/`conceptReference`). Kept where already specific: `alter_nodes` blank blob stays D29 (MAKE_INPUT_INVALID for MAKE) but now also carries `missingParameters`. Also fixed in passing: `query_structure`/`alter_structure` unknown-operation rejections were unrecorded (D26). Guards: `McpToolParameterOptionalityTest` (no parameter without a default, discovered from `plugin.xml`; no near-miss is a real parameter of its tool; no stale entry), bridge-level tests per tool, `McpCallLogTest` cases. **D**: `reference-formats.md` no longer claims the old behaviour and maps the top-level naming conventions; the `create_model` call sites in 12 SKILL.md files and all 16 `aspect-model-stereotypes.md` copies now name `moduleName`. **Measurement, read before comparing round 10**: `analyze_runs.py` counts the new envelope as `arg_validation_errors`, so the column stays comparable; but an *unknown* operation sent without a blob, formerly a binder missing-`parameters` error, is now an uncounted `unknownOperation`; `inventorySha256` and schema bytes move. Out of scope: JSON `null` on a `String` parameter still dies in the platform decoder; blank-`operation` retries list every enum constant, including `query_structure`'s deprecated `FIND_INSTANCES` (pre-existing in `unknownOperation`) |
| D44 | `mps-aspect-*` `references/*.md` | Every large reference file opens with a `## Contents / when to read what` block, and agents do not use it: they run `grep -n '^## '` to rebuild the same index, then a ranged `Read`. The block costs bytes in each read and saves no turn. 45 of S1's 131 tool calls (34 %) were skill navigation. | `S1-sonnet-1:27-33` (round 8) | **fixed** (2026-09-24, MPS-40191) — **T**: 18 lookup references over 12 KB split into `references/<stem>/<slug>.md` (127 section files, largest 8.5 KB), each old path rewritten as a small index table so the ~140 whole-file mentions across the catalog and the 9 Kotlin tool-description citations stay valid; `mps-aspect-constraints` and `mps-aspect-structure-concepts` Section indexes map each job straight to its file(s), and the `grep -n '^## '` / offset-limit instructions and all four `## Contents / when to read what` blocks are gone. Kept whole by design: `dot-expression-basics.md` (the "Minimum reading set" one-file rule; Contents block dropped) and `mcp-tools-index.md`. Section-specific citations (§"…" / → "heading", 9 sites) and cross-file "see above/below" prose (17 sites, 5 of them found in review) retargeted to the section file. Guard: `SkillReferenceLayoutTest` — 12 KB budget with a 3-entry allow-list, no heading-index instructions, every reference reachable from its `SKILL.md`, and `validate_skill_catalog.py` now runs in the suite. **Measure in round 10:** zero `grep` against `references/` in S1/S2/S4; S1 skill-navigation calls ≤ 25 (was 45); skill bytes/turn flat or lower. |
| D45 | `parse_java_and_insert` | `featureKind: STATEMENTS` into a `*Function` `body` role fails because the role expects a `StatementList`; the caller must insert the `StatementList` first and re-target its `statement` role. The error text says so clearly, but the two-step dance is mechanical. | `S2-sonnet-1:53-55` (round 8) | **fixed** (2026-09-23) — **S**: `parse_java_and_insert` now auto-wraps. `insertAsChild` redirects statements into the role's `StatementList` — reusing an existing one and **appending** (a body role always holds one, and overwriting it the way the single-cardinality branch overwrites an occupant would silently discard a method body), creating one only when the role is empty, so `position`/`index` then refer to the position inside the list; `insertAsReplace` wraps into a new `StatementList` and replaces (the one case where multi-node input is accepted there, since the statements collapse into a single node). The gate is structural, not `featureKind`-based, so `EXPRESSION` into `body` is still rejected; every wrap is reported in `warnings`. `wrapAsConsoleCommand`, which already did this for the console path, now shares the helper. **D**: `mps-baselanguage` `parse-java-tips.md` (the wrap, its two modes, the `position` shift) + `critical-rules.md` (blueprints still never wrap) + the tool description. |
| D46 | `mps_mcp_update_module` + `mps-aspect-migrations` / `mps-aspect-accessories` skills | No tool exposed a language's own `version` int (the field that gates `MigrationScript.fromVersion`, distinct from the consumer-side `languageVersions` stamps): `mps_mcp_update_module` supported only RENAME/CHANGE_VIRTUAL_FOLDER/DELETE, no structure-aspect operation covered it, and no read path emitted it — only the consumer stamps under `usedLanguages`, which is what made the worker mistake one for the other. The skill told the worker to hand-edit the `.mpl`, which cannot work (MPS holds the descriptor in memory and rewrites the file). The console-reflection workaround also failed: it does not resolve in the ephemeral `TempModule` (no `MPS.Core` dependency, and the console module is not addressable by `mps_mcp_module_dependency`), and routing it through a real project module compiled clean but then silently no-op'd via `mps_mcp_run_console_command` (`{"executed":true}`, no history entry, version stays 0). Only the task's own "stop and explain if blocked" instruction ended the retry loop (5 `run_console_command`/`get_console_history` round trips, 3 `sleep` polls, 18 skill-doc greps); first-ever S4 measurement (round 9), task **FAILED** on this alone. **Corrections found while fixing** — (a) the API is trivial (`Language.getLanguageVersion()`/`setLanguageVersion(int)`, `core/kernel/source/jetbrains/mps/smodel/Language.java:278,282`), so the gap was purely surface-level; (b) there is no "New Migration Script wizard" — the bump comes from a lightweight-DSL node factory (`Migration_Queries.init`) that fires only on the `create_root_node` path, see D47; (c) the `FixLanguageVersion` quick fix is **dead code** — `MigrationsCheckUtil.checkMigrationsVersions` reports through `reportTypeError(..., null)` with no intention provider, so nothing references the quick-fix node and `check_root_node_problems(autoApplyQuickFixes=true)` can never repair a version mismatch; (d) the version is **also baked into the generated `LanguageRuntime`** (`MigrationScriptCollector.fillFor` reads `lr.getVersion()`), so a descriptor-only bump satisfies the checker but the migration is still never offered until the language module is rebuilt — a second silent-mismatch trap. | `S4-sonnet-1:29-35` (skill search), `S4-sonnet-1:96-127` (console attempts + stop); `HOTSPOT_REPORT_round9.md` | **fixed** (2026-09-24, MPS-40199; first cut 2026-09-23) — the first cut added an explicit `SET_VERSION` (a value or +1); it was **replaced** because a language's version is not a free value: with migration units present the only valid version is `max(fromVersion) + 1`, enforced by `MigrationScriptVersions` and by the migration generator, and every MPS writer except the Properties dialog derives it (the `MigrationScript` factory, `LanguageStructureMigrationParticipant`, `CorrectLanguageVersion_Action`). A setter only added ways to break that (non-idempotent +1 on retry; a misspelled key silently becoming a bump). **S**: (1) `mps_mcp_update_module` **`SYNC_VERSION`** (0004712949b8), the headless "Correct Language Version": no value, idempotent, no-op without a versioned unit, reports `migrationProblems` (unset / duplicate / missing version), warns on lowering and names consumers stamped above the target, never writes consumer stamps, keeps the `runtimeStale` diagnostics; `fromVersion` is read through the `IMigrationUnit` behavior methods, so mcp-tools gains no dependency on the migration language. The read path (`languageVersion` in module info and `get_project_structure`) stays. (2) **MPS side**, `jetbrains.mps.lang.migration`: a `PureMigrationScript` node factory (`MigrationUnit_factories`) mirrors `Migration_Queries.init` (fromVersion = current, bump; a replaced versioned unit's fromVersion is inherited without a bump; `SNodeBuilder`, copies and dry runs run no factory, so refactorings still bump once); and the **`FixLanguageVersion` quick fix is reachable again, in its original form** — attached, not auto-applied, to the mismatch error with `wanted = max(fromVersion) + 1`, via a `MigrationsCheckUtil` overload that leaves the generator's call and output unchanged. **History found while fixing** (no recorded reason in git or YouTrack): `43a6488584f3` (2014-08-29) added the action and `IMigrationUnit`; `8392df3f28dd` (2014-09-11) added the rule on `MigrationScript` with the quick fix; `ba21a5da9859` (2014-12-17) moved the checks into a string-returning util shared with the generator and so lost the wiring; `RefactoringStep` became an `IMigrationUnit` in `37d9a2c7244c` and the rule was widened in `e17d042d7486` (2015-09), and was split into `PureMigrationScript` / `RefactoringLog` in `bdceb832a624` (2015-10-15) — so `PureMigrationScript` never had the quick fix, nor a factory (in the IDE it comes from refactorings that bump themselves; MCP structure edits bypass them). Correction (c) above therefore describes a regression, not dead code. **D**: `mps-aspect-migrations` / `-accessories` / `mps-node-editing` / `mps-mcp-workflow` describe factory + `SYNC_VERSION` (quick fix as the explicit alternative) instead of manual version arithmetic; `mps-aspect-actions` corrected — `NodeFactoryManager.setupNode` runs every factory up the super-concept and interface chain, not only the exact `applicableConcept`. Tests: `JetBrainsMPSModuleMcpToolsetIntegrationTest` (SYNC_VERSION, quick fix), `NodeFactoryOnBlueprintPathIntegrationTest` (PureMigrationScript factory). **Measure in round 10:** S4 passes the version-bump criterion with no `SYNC_VERSION` after creating a script and no manual version edits. |
| D47 | `mps_mcp_insert_root_node_from_json` (`AbstractNodeOps.kt:172-173`) | **Node factories are run without a target model on the JSON-blueprint insert path, and whatever they build is then deleted.** `mps_mcp_create_root_node` uses `SNodeFactoryOperations.createNewRootNode(model, concept, null)` (`JetBrainsMPSRootNodeMcpToolset.kt:433`), so `NodeFactoryManager.setupNode` — which walks super-concepts *and* implemented interfaces — invokes each `NodeFactory.setup(node, sample, enclosing, index, model)` with a real `model`. `insert_root_node_from_json` instead calls the 2-arg `SNodeFactoryOperations.createNewNode(sConcept, null)` overload, which forwards `model = null` and `enclosingNode = null`, and the very next line (`AbstractNodeOps.kt:173`) deletes every child the factory produced. For `MigrationScript` (an `AutoInitDSLClass`) the factory is `Migration_Queries.init`, which would set `fromVersion` to the language's current version and bump the language version; with `model = null` it NPEs on `futureModel.getModule()`, the NPE is swallowed by the DSL descriptor's `catch (Exception e) { e.printStackTrace(); }`, and the caller sees an ordinary success envelope with no `fromVersion` and no bump. This is almost certainly the mechanism behind the S4 failure recorded in D46, since `mps-aspect-migrations` routes agents to `references/json-blueprints.md` → `insert_root_node_from_json`. Affects every `AutoInitDSLClass` / factory-initialised concept, not just migrations. | found while fixing D46 (2026-09-23); `AbstractNodeOps.kt:172-173`, `SNodeFactoryOperations.java:49-53`, `NodeFactoryManager.java:131-146`, `DSLDescriptor__BehaviorDescriptor.java:47-65`, `Migration_Queries.java:27-40` | **fixed** (2026-09-23) — **two symptoms beyond the migration case the entry describes**, same root cause: the nulls also (a) destroyed factory output that *had* succeeded (`ClassLikeInitHelper.init` adds DSL members to `member` before the NPE, and line 173 deleted them — the reachable instance of this is `jetbrains.mps.lang.behavior`'s `ConceptMethod` factory, whose `visibility` child was silently dropped from every blueprint-added behavior method), and (b) left `enclosingNode` null, which that same factory needs to mark methods on an `InterfaceConceptDeclaration` abstract/virtual — while `alter_structure`, which goes through `addNewChild`, passed both all along. Every factory on these paths is null-tolerant, so all of it was silent. **Claim checked and withdrawn:** the entry's `conceptId`/`propertyId` were *not* affected. `new SNode(SConcept)` calls `SModel.generateUniqueId()` (`SNode.java:99-101`), so a detached node already has a regular id and `ConceptIdHelper` never fell back to `randomLong()`; the model/enclosing node only feed its collision scan, which is not reachably observable. **Verified per component** by re-running the suite with each half of the fix backed out in turn — three of the eight new tests are isolated regressions, one per component: withholding the model from `setupNode` fails only `inserting a MigrationScript from a blueprint sets fromVersion and bumps the language`; restoring the old `createNewNode` + blanket delete fails `update_node ADD CHILD keeps the visibility child the behavior factory creates` and `behavior factory sees the enclosing behavior root and marks interface methods abstract`. The other five are invariant pins (id conventions, omitted-mandatory-role scope limit, dry-run inertness). Full suite green at 897/897. **S**: `instantiateNode` now calls `SModelOperations.createNewNode(model, null, concept)` + `NodeFactoryManager.setupNode(concept, node, null, enclosingNode, model)`; `enclosingNode` is threaded from the three call sites that know the parent and from the recursive child call; the blanket child delete is replaced by per-role clearing (the blueprint owns the roles it names, the factory owns the rest); `createNodeStructure` is deliberately *not* run, so mandatory roles stay empty as before; factory throws become a `warnings` entry instead of a failure; and `warnings` now reach the response on the real (non-`dryRun`) path of `insert_root_node_from_json`, `update_root_node_from_json`, `update_node` ADD/SET CHILD and `insert_console_command_from_json`. `dryRun` withholds the model, since factory side effects hit the model and module and nothing rolls them back. **D**: `mps-node-editing` gains a "Node factories" section with the blueprint-vs-factory composition table (it previously said nothing about factories at all, which is why the gap was invisible); the D46 wording in `mps-aspect-migrations` SKILL + `json-blueprints.md` + `form-selection.md` is **reversed** — the factory now does the `fromVersion` + version bump, so an agent following the old text would double-bump. Regression tests: `NodeFactoryOnBlueprintPathIntegrationTest`. **MPS side** (the other half of "stop swallowing factory exceptions"): `DSLDescriptor.initializeInstance` in `jetbrains.mps.baseLanguage.lightweightdsl`'s behavior aspect ended in `catch (Exception e) { e.printStackTrace(); }`, which sits *inside* the factory and so swallowed the failure before the new MCP-side try/catch could ever see it — for the migration case specifically, a broken initializer still produced a clean success envelope with no warning. Replaced with the language's own `log error "…", e` (`jetbrains.mps.baseLanguage.logging.LogLowLevelStatement`, already in scope via the `general-purpose` devkit), generating the standard `if (LOG.isErrorLevel()) LOG.error(msg, e)` used elsewhere in MPS model code. Deliberately **not** rethrown: `initializeInstance` also runs on the interactive editor path, where a throw would break node creation for every lightweight DSL, so the failure is now diagnosable in `idea.log` rather than fatal. Language rebuilt and the regenerated `DSLDescriptor__BehaviorDescriptor.java` committed with it per `.agents/conventions.md`. **Subagent review caught four things worth recording** (all fixed): (1) the docs rewrite was wrong for `PureMigrationScript` — it carries no `AutoInitDSLClass` parent (`StructureAspectDescriptor.java:480-492`) and the migration language registers factories only for `ConsequenceFunction`/`ReflectionNodeReference`, so the *most common* migration form still needs a manual `fromVersion` + `SET_VERSION`; the advice is now split per form, and the factory's `isAspect(futureModel, "migration")` guard is documented; (2) `instantiateNode` applied children *before* references, so a nested child's factory saw a reference-less parent — the `ConceptMethod` factory follows `ConceptBehavior.concept`, so a one-call blueprint silently lost `isAbstract`/`isVirtual` where the same content split across two calls worked. References now go first; the accepted trade-off is that a name-resolved reference can no longer see this node's own children, which fails loudly instead of silently. New regression test `a one-call blueprint gives a nested child's factory the parent's references`, verified genuine by restoring the old order — it is then the only test in the class that fails; (3) "a failed factory is reported" was false for exactly the `AutoInitDSLClass` family D47 is about — the new try/catch only fires on a *throw*, and those initializers swallow internally, so the docs now say a clean envelope is not proof and to verify the state; (4) factory side effects survive a failed batch, so `insert_root_node_from_json`'s "atomically with batch rollback" description overstated the guarantee and is now qualified. Also fixed: warnings were dropped on error envelopes (`errJson` takes them and none were passed), cancellation is now rethrown from the factory catch, and two doc pointers were wrong (`get_concept_details` does not list aspects — `query_structure LIST_CONCEPT_ASPECTS` does; `update_root_node_from_json` runs no factory for the root itself). |
| D57 | `study/scenarios/S1/done_criteria.md` criterion 6 | The criterion asks the evaluator to read `descriptorStatus` from `get_concept_details` without saying that the field is emitted **only** when a descriptor is hollow (`AbstractOps.kt:3285`, `mps-language-analysis/references/concept-details.md:95`). A healthy language therefore returns no such field, and both round-12 evaluators reported the criterion as unverifiable/"inferred". Study asset, not a product defect. | round 12: `S1-opus-1.eval.md`, `S1-sonnet-1.eval.md` (criterion 6). | **fixed 2026-09-25** — criterion 6 now names the call and says `descriptorStatus` is present only for a hollow descriptor, so "no entry has the field" is the pass condition. Evaluator-side only: `worker_prompt.md` and its `promptSha256` are unchanged, so rounds stay comparable (lesson 25). |
| D49 | `mps_mcp_alter_nodes` `MOVE_CHILD`. Not MOVE_CHILD alone: every `parameters` operation with more than one required key, or with a choice between keys: IS_SUBCONCEPT_OF, GET_ASSIGNABLE_REFERENCES, GET_ENUMERATION_LITERALS' property form, CREATE_CONCEPTS, CREATE_ENUM, UPDATE_CONCEPT_*, RENAME_CONCEPT_*, MOVE_NODE_TO_PARENT. | Missing parameters are disclosed one at a time, so learning both required parameters costs two sequential retries. Round 10's "unlike every other operation" did not hold: round 10 did not exercise the others. `AbstractOps.rejectMissingParameters` (D43) covers top-level tool parameters only. Every blob read answered `Parameter 'X' is missing` for the first absent key (GET_ASSIGNABLE_REFERENCES without even a `code`), and CREATE_CONCEPTS and MOVE_NODE_TO_PARENT disclosed their choice of input only after the other keys. | round 10: `S2-sonnet-1:34` (missing `nodeReference`) → `:35` (now missing `childRole`) → `:36` succeeds. Round 11 §5: still unfiled. | **fixed 2026-09-25**, not yet measured. Each operation's `ParameterKeys` table declares its requirements: `required(key, expected)`; `requiredOneOf(…)` for a choice, named `one of a/b`; and `onlyWith` / `unlessWith` for a key that goes with another key or is replaced by it. The four dispatchers check them in one pass after the unknown-key check. One `INVALID_REQUEST` names every unmet requirement, with `details.missingParameters`, and its sentence comes from the private `retrySentence` that the top-level `missingParametersMessage` also uses. `update_node`'s ADD CHILD list-every-key helper and its SET CHILD single-key helper (bespoke copies, not one-at-a-time defects) now go through `rejectMissingParameters(…, operation = …)`. SET PROPERTY and SET REFERENCE, whose values are lists with no blank to test, use its `missingParametersResponse(…)`. As for other top-level parameters, a blank `nodeReference`, `childRole`, `childJson` or `childNodeRef` there now counts as missing. Option D is kept for blob strings. A choice appears in `details.missingParameters` under its `one of a/b` name. Not covered: the MAKE operation (its own `MAKE_INPUT_INVALID` rejection) and the separate tool `parse_java_and_insert` (blueprint parser, one key per call: `Missing 'parameters.<key>'`, `'<key>' is required for <mode>`). Side effect: `analyze_runs.py` `arg_validation_errors` now also counts blob-key omissions. Re-measure: no sequential missing-key retries on the four operation tools. |
| D51 | `mps-baselanguage`: `SKILL.md` Critical Directives, `references/json-patterns.md`, `references/concept-mapping.md`, `references/critical-rules.md`; `mps-model-manipulation`: `references/property-and-mutation-ops.md`, `references/closures-catalog.md` | First filed against `mps-model-manipulation` / `mps-quotations`; neither is the right home (quotations were not used, and the collections creators in `mps-model-manipulation` already say "all wrapped in `GenericNewExpression`"). A Java array creation with initializer (`new String[]{""}`, not a collection literal) built directly as `ReturnStatement.expression` is rejected: `ArrayCreatorWithInitializer` is an `AbstractCreator`, not an `Expression`, and must be the `creator` child of a `GenericNewExpression`. The wrapper was documented, but only per creator (`ClassCreator`, `ArrayCreator`, `AnonymousClassCreator`). `ArrayCreatorWithInitializer` appeared nowhere and the general rule was never stated. The worker had read `json-patterns.md`, including its `new int[50]` wrapper blueprint, at `:27` and still assumed the with-initializer variant stands alone. `mps-model-manipulation` (loaded at `:41`) made the same error itself: it documented `SNodeCreator` (`new node<C>()`, which extends `AbstractCreator`) as a top-level `Expression` with an unwrapped blueprint. D43's near-miss vocabulary covers parameter names, not AST shape, so recovery was ad-hoc discovery — about 5 extra calls. | round 11: `S6-sonnet-1:57` rejected; `:58`–`:59` `get_concept_details` on the rejected concept and siblings; `:60`–`:62` `ToolSearch` + `search_concepts` + `get_concept_details` to find `GenericNewExpression`; `:63` succeeds. | **fixed (docs) 2026-09-25**, not yet measured. `mps-baselanguage/SKILL.md` has a "`new …` is a `GenericNewExpression` plus a creator" directive listing the creator kinds, including smodel `SNodeCreator`; `json-patterns.md` has an "Array with Initializer" blueprint; `concept-mapping.md` has the `new T[]{a, b}` row; `critical-rules.md` extends the wrapping rule to every `AbstractCreator`; `property-and-mutation-ops.md` wraps its `SNodeCreator` blueprint; `closures-catalog.md` names `SequenceCreator`. Not covered: round 12's `AnonymousClassCreator` instance (`S1-sonnet-1:73`, constraints). That worker loaded only `mps-mcp-workflow`, `mps-language-aspects-overview`, `mps-aspect-structure-concepts`, `mps-aspect-editor` and `mps-aspect-constraints`, and had already read the wrapped form in `computed-sequences-blueprint.md`, so more text is unlikely to help there; it is the case for the **S** wrapper hint, **implemented 2026-09-25, not yet measured** ([plan](../docs/d51-d52-assignability-hints-plan.md), [task list](../docs/d51-d52-assignability-hints-task-list.md)): a creator rejected in an `Expression` role now names `GenericNewExpression.creator` and the JSON to paste (`details.wrapperCandidates`). Re-measure S6 (no `ArrayCreatorWithInitializer` rejection) and S1 (creator rejections); `analyze_runs.py` counts them in `assignability_errors`, `assignability_hinted`, `assignability_recovery_calls` and `assignability_unrecovered`. Target: 5 recovery calls → 0. |
| D52 | `mps-model-manipulation`: `SKILL.md` Critical Directives, `references/dot-expression-basics.md`, `references/property-and-mutation-ops.md`; `mps-language-analysis/references/concept-details.md`; `mps_mcp_get_concept_details` tool description (`JetBrainsMPSLanguageMcpToolset.kt`) | First filed against `mps-model-manipulation` / `mps-quotations` as an undocumented rule. A JSON-blueprint `SPropertyAccess.property` reference pointed at the enclosing `ConceptDeclaration` (`Recipe`) instead of the `PropertyDeclaration` (`name`). The rule the entry first proposed stating was already documented: the `<PropertyDeclaration-noderef>` placeholder in `property-and-mutation-ops.md`, `mps-aspect-behavior` `constructors.md`, `mps-aspect-actions` `SKILL.md`. The server error names the expected concept too. The same blueprint wired `SLinkListAccess.link` to `steps` correctly (ref copied from `:98`'s `targetReference`); only the inherited `name` got the concept ref. So the decisive gap is the inherited feature: no skill said that `name` (from `INamedConcept`) is declared in `jetbrains.mps.lang.core.structure`, not in the language, or which field carries its ref. `concept-details.md` and the tool description both pointed the wrong way: `featureId` for smodel `SPropertyAccess` / `SLinkAccess` targets, `sourceNode` "informational only". A `featureId` has neither an `r:`/`i:` prefix nor a `.`, so `AbstractNodeOps.resolveReferenceTarget` does not treat it as a node ref, and the blueprint stores a dynamic reference that never resolves — silently, since the insert and its dry run both succeed. The right `sourceNode` had been in the worker's context since `:38`/`:55`. About 4 extra calls on top of the direct-JSON-too-large redirect that preceded it. | round 11: `S6-sonnet-1:109`–`:110` temp-file redirect; `:111` rejected; `:112`–`:113` `print_node` on the wrong and the right target; `:114` edit to the `name` `sourceNode` (`…(jetbrains.mps.lang.core.structure)/1169194664001`); `:115` succeeds. | **fixed (docs) 2026-09-25**, not yet measured. `mps-model-manipulation/SKILL.md` has a "Feature references take the declaration's `sourceNode`" directive, with the fixed `name` ref; `dot-expression-basics.md` and `property-and-mutation-ops.md` say the same at their `<…Declaration-noderef>` placeholders; `concept-details.md` and the `get_concept_details` tool description now say `featureId` is not a node ref and `sourceNode` is the smodel feature-access target (the description change moves `inventorySha256`). Re-measure on the next S6 run: no `SPropertyAccess.property` rejection. **S**, **implemented 2026-09-25, not yet measured** ([plan](../docs/d51-d52-assignability-hints-plan.md), [task list](../docs/d51-d52-assignability-hints-task-list.md)): a concept given as a feature-access target now gets its own and inherited feature declarations listed with their refs (`details.targetCandidates`), and a `featureId` given as a reference target is rejected, naming the declaration's `sourceNode`, instead of stored silently. Re-measure with the same `analyze_runs.py` columns `assignability_errors`, `assignability_hinted`, `assignability_recovery_calls` and `assignability_unrecovered`. Target: the corrected call follows the rejection directly, with no `print_node` lookups. |
| D55 | `mps_mcp_query_structure` `LIST_CONCEPT_ASPECTS` | This operation uniquely takes a singular `conceptRef`. Sibling tools converged on plural `conceptRefs` after D43. The near-miss message names the fix and the worker self-corrected in 1 retry, so the turn cost is low, but the naming surface D43 was meant to unify still has this one holdout. | round 11: `S8-sonnet-1:48`→`:49`. | **fixed 2026-09-25**, not yet measured. "Uniquely" was too narrow: every single-concept `query_structure` operation took only `conceptRef` (IS_SUBCONCEPT_OF, GET_SUB_CONCEPTS, GET_ASSIGNABLE_CONCEPTS, GET_ALL_SUPERCONCEPTS, LIST_CONCEPT_ASPECTS, IS_SMART_REFERENCE), and all six now also accept `conceptRefs`. The mechanism is the blob-key alias one: a `BlobKey` may carry a `plural` spelling (`PARAM_CONCEPT_REF.withPlural(PARAM_CONCEPT_REFS)`), and `collapsePluralParameterKeys`, run after the missing-key check, reduces it to one string. The plural takes FIND_INSTANCES' shape (a nonblank string or a nonempty array of nonblank strings, every element checked) and none of the singular key's legacy `asString` leniency. With more than one value, the response gets a `warnings` entry naming the value used and the count ignored. That includes a failure thrown by a later typed read, which the dispatcher now catches itself. `conceptRef` beside `conceptRefs` is the usual two-spellings rejection. `alter_structure` keeps the strict key, so a write is never applied to only the first of several concepts; `conceptRefs` there still gets "did you mean 'conceptRef'?". Documented in `mps-mcp-workflow` `request-conventions.md`, `mps-aspect-structure-concepts` `tool-conventions.md`, and the `mps_mcp_query_structure` tool description, which now also says a success response may carry `warnings` (the description change moves `inventorySha256`). Re-measure: no `conceptRefs` retry on the next S8 run. |
| D56 | `mps_mcp_get_concept_details` near-miss list; `mps_mcp_print_node` parameter name | Filed from rounds 10–11: `concept` / `conceptName` were outside the `get_concept_details` near-miss list, and the worker was said to repeat the wrong key on a second concept before switching to `conceptRefs`; `print_node` `node` was said to show the same two-in-a-row miss. | round 10 `S2-sonnet-1:118–120`; round 11 `S6-sonnet-1:10–16`; round 12 `S1-sonnet-1:65–68` | **closed 2026-09-25 — premise not reproduced.** Every "repeated miss" was one parallel batch, sent before any error came back, and the next turn fixed every call in it. All 10 wrong-key batches in the study recovered in one turn, except at baseline, whose message named no key at all. Whether the message named the key actually sent made no difference: in rounds 4–5 `conceptRef` was not yet listed, and batches of 8 and 4 still recovered in one turn each. `print_node` already named `nodeReference` and listed `node` (D43). The instance count was also off: round 8 was `conceptNames`, and round 10 S1 was the covered `conceptRef`. A tidiness edit only, with no turn saving expected: the rejection now takes its spellings from `RequiredParameterNearMisses` (new `conceptRefs` / `languageRefs` entries, adding `concept`, `conceptName` and `conceptNames`), which `McpToolParameterOptionalityTest` guards. D27's test, renamed `get-concept-details rejects every near-miss with a copy-pasteable retry line`, loops over all seven spellings. The tool description did not change, so `inventorySha256` did not move. The misreading came from `analyze_runs.py`'s error→retry pairing, which ignores the batch (31 of 107 pairs across all rounds are same-batch); that is not fixed here. Analysis: [`../docs/d56-near-miss-validation.md`](../docs/d56-near-miss-validation.md) |
| D58 | `mps-mcp-workflow/scripts/mps_dump.py` | A subcommand run on the wrong kind of dump fails silently: `shape` (meant for a `get_concept_details` result) on a `print_node` node dump printed `{"concepts":0,"features":0,…,"lines":0}` and exited 0. The worker could not tell "wrong file kind" from "nothing to show", so it ran `--help` and then `node`: 2 wasted calls. | round 13: `S1-sonnet-1:28` (`print_node` envelope), `:29` (`shape`, empty result), `:30` (`--help`), `:31` (`node`, correct). | **fixed 2026-09-25** (`80d98c96cd89`, round-13 remedy R-e), not yet measured. A new `dump_kind()` classifies the input (concept-details, node, model, module, project). `shape` on anything but a concept dump, and `roots` / `node` / `count` on a concept dump, now exit 3 (bad input) with a message naming the kind and the subcommand that reads it, e.g. `no concept entries for \`shape\`: this looks like a print_node / get_project_structure node dump; use \`node\` instead`. A right-kind dump with zero matches still exits 0 with a zero count. Verified by hand on the bundled examples: the five wrong-kind cases exit 3 and name the right subcommand, `roots --concept NoSuchConcept` exits 0 with `roots: 0`, and `shape` on a concept dump still works. `SkillScriptsDriftTest` gained the same cases; passed 2026-09-25 (`McpToolsIntegrationTestSuite`: 1006 finished, 0 failed, 0 ignored). The script's docstring documents the exit code. Re-measure: no `--help` or second-subcommand retry after a `mps_dump.py` call. |
| D60 | `mps-node-editing/SKILL.md`, `scripts/table_to_bulk_insert.py` section | The skill documents the script's usage (`SKILL.md:154–161`), but does not say that the usage block is the contract. The worker read the 12.9 KB source and both example files before running it, and then used it correctly the first time, so the reads bought nothing. | round 13: `S3-sonnet-1:17` (source), `:18–:19` (examples), `:22` (correct first run). Part of S3-sonnet's skill bytes doubling from 34 KB (round 12) to 68 KB. | **fixed (docs) 2026-09-25** (`80d98c96cd89`, round-13 remedy R-g), not yet measured. `mps-node-editing/SKILL.md` now ends the usage block with: "This usage block plus `--help` are the contract: there is no need to read the script source or the examples first." It is generic and names nothing from S3 (lesson 19). The three catalog copies are identical. Re-measure on the next S3-sonnet run: no Read of `table_to_bulk_insert.py` or `scripts/examples/` before the first run. |
| D59 | `mps_mcp_get_project_structure` NOT_FOUND (`JetBrainsMPSProjectMcpToolset.kt`) | With `includeStubModules=false`, a `startingPoint` that names a stub or library **model** (`jetbrains.mps.scope`) got a bare "Starting point '…' not found". The worker guessed the flag correctly, but the error gave it no lead. The filed claim that a library **module** already got the "filtered out" hint was wrong: `resolveModule(…, projectOnly = true)` returns only project modules, the same set `isModuleInSelectedProject` checks, so that branch could not fire and library modules got the bare message too. | round 13: `S1-sonnet-1:52`→`:53` (same call plus `includeStubModules: true`, succeeded). Opus reached the same model via `includeDependencies` on its own constraints model (`S1-opus-1:58`). One occurrence. | **fixed 2026-09-25** (`80d98c96cd89`, round-13 remedy R-f), not yet measured. Before the final NOT_FOUND, a name that resolves anywhere in the repository to a model or module reports "resolved to a non-project model/module and was filtered out. Set 'includeStubModules' to true …" (`filteredOutStartingPoint`). The error code stays NOT_FOUND, and a genuinely unknown name keeps the plain message. Covered by `JetBrainsMPSProjectMcpToolsetIntegrationTest` (library model and library module, each also succeeding with `includeStubModules: true`; the unknown-name test asserts no "filtered out"); passed 2026-09-25. Not covered, and not observed: a node or `model.RootName` starting point inside a library model still gets the bare message. The now-unreachable module branch before the new check was left in place. Re-measure: no `includeStubModules` retry after a NOT_FOUND on the next S1 run. |
| D54 | console response printing; `mps-console`; `get_console_history` | A query that returns a sequence executes correctly and is recorded in console history in the right order, but the printed response is a `NodeWithClosure` whose text is only an item-count summary (`"3 nodes"`), not the item values. The done-criteria check that the printed items include all 3 Ingredient names could only be partially verified: the count matches, the names do not appear. An agent or a human that needs the values back through the read-only console path has to shape the query (iterate and `#print` per item) rather than rely on `get_console_history`. | round 11: `S9-sonnet-1` (first measurement of S9); `#instances(Ingredient)` and its filtered variant, verified live before the project closed. | **fixed 2026-09-25**, not yet measured. Verified live on 37 `Plan` nodes: a sequence or list of nodes prints as a `NodeWithClosure` count (`37 nodes`, also with `.toList`), a sequence of strings as one `TextResponseItem` (`[a, b, …]`), and a single node as a `NodeReferencePresentation` whose `target` is the real node. The per-item `#print it.name` workaround proposed here works but separates items only by spaces, so multi-word names cannot be split; it is documented as the thing not to do. Three parts. **Study**: S9's criterion could never pass, since unfiltered `#instances(Ingredient)` always prints `3 nodes`. That check now expects `3 nodes`, and step 2 adds `.select({~it => it.name})`, whose response must list the 3 names; check 3 (recall the unfiltered query) only gains "or `select`". S9 results up to and including round 11 (the old criterion) are not comparable on check 2 with runs after this change. **D**: `mps-console` `SKILL.md` has a "Reading a command's output" section (history → response `reference` → `print_node` `PLAIN TEXT`, or `JSON` `deep` for node refs; the value-type table; `.select` for values, `forEach(#print it)` for refs), with pointers from `console-languages.md` and `mcp-tools-index.md`. **S**: `kind:response` entries were bare envelopes with no text, so `get_console_history` never returned any output; they now carry the same 200-char `preview` as commands, skipped above 100 printed items because it renders the whole response (`JetBrainsMPSConsoleMcpToolset.kt`). The tool description says which sequences read as a count (nodes, references, models, modules; `empty sequence` when empty) and that a command printing nothing has no response entry, both verified live. Re-measure, on a plugin build that includes the `preview` change: the next S9 run reads both responses from history without a `print_node` detour and passes check 2. **Round 14 follow-up (remedy R-a, 2026-09-25), not yet measured:** check 2 now passes, but both workers still printed each response to get "the exact text", because nothing said when the preview already is the exact text. **S**: every `preview` (command and response) now carries `previewComplete`. It is true when nothing was cut and the rendering, once trimmed, had no line breaks or whitespace runs to collapse, so the preview equals `print_node` `PLAIN TEXT` without the Console's padding. That was checked live: `  37 nodes  ` versus the preview `37 nodes`; a 45-item alias list was cut, so it is false. Two short responses keep the flag honest too: `#print "alpha  beta"; #print "gamma";` renders as `  alpha  beta gamma  ` but previews as `alpha beta gamma`, and `"one\ntwo"` (a `NewLineResponseItem`) renders as `  one  \ntwo  ` but previews as `one two`. Both compute false. Consecutive `#print`s join with one space, not a line break. The flag was then verified live on a restarted MPS running this build: `get_console_history` reported true for the count, the one-line commands, a `.select` of one name (`[BooleanConstant]`), and 12.06's `hello from MCP test`, and false for the cut list and both short cases. The tool description says to skip `print_node` when it is true. **D**: step 2 of `SKILL.md` "Reading a command's output" branches on `previewComplete`; `mcp-tools-index.md` names the flag; `12_console.md` 12.06 expects it `true`. Re-measure on a plugin build with the flag: 0 `print_node` calls on `kind:response` references in S9 (round 14: 2 per run). |
| D61 | `mps-console/references/mcp-insertion.md` (hands off at `:97`); the pieces are spread over `mps-model-manipulation/references/{dot-expression-basics,property-and-mutation-ops,closures-catalog,golden-rules-and-pitfalls}.md` | No complete blueprint for the Console's typical job: a query with a closure, `#instances(C).where({~it => <predicate on it.prop>}).select({~it => it.prop})`. Workers put it together from four files in two skills. The `String.length()` JDK-stub call exists only by analogy with `dot-expression-basics.md:383` (`~Iterable.iterator%28%29`). Both models then miscounted brackets in a hand-written ~3.9 KB heredoc and regenerated the blueprint with a Python builder. The dry run's unresolved-`it` warnings have to be reasoned away each time. | round 14: `S9-opus-1:9–14, :17–20` (10 calls, retry `:19`→`:20`); `S9-sonnet-1:4–9, :12–20, :26–28` (18 calls, including whole-file reads of `dot-expression-basics.md` (24 KB) and `closures-catalog.md`, retry `:26`→`:27`); round 11: `S9-sonnet-1:14–24, :28` (server `EOFException` on the same bracket miscount) | **fixed (docs) 2026-09-25** (round-14 remedy R-b), not yet measured. `mcp-insertion.md` has a section "A query with closures" with a generator script instead of raw JSON. Raw JSON for this shape is 3.8 KB with brackets nested 39 deep, and hand-writing it was the failure, so the script is safer to copy. It uses `CUSTOMIZE` constants for the concept, the filter and select properties (the property target is `get_concept_details`'s `sourceNode`), and one JDK `String` method (the `~String.<m>%28<types>%29` form, with an argument). The section also lists what to expect: two dry-run `it` warnings, one per closure, a clean `check_root_node_problems`, the printed command, and the response shape. It was verified live in this checkout: the script output equals a blueprint that passed dry run, insert, and a clean check, and ran, printing a bracketed list. After an MPS restart it was run again through the MCP tools, straight from the installed `.claude/skills` copy, with the same result. A variant customised the way an agent would (`startsWith("BooleanC")`, select `name`) worked first time and printed `[BooleanConstant]` with `previewComplete:true`. For lesson 19 the domain is `#instances(ConceptDeclaration).where(name.endsWith("Literal")).select(conceptAlias)`: not `Ingredient`, not `length`, and not a comparison operator. `mps-console/SKILL.md` way #3 points at the section. Re-measure on the next S9 run: at most ~4 calls from the first skill read to a successful dry run of query 2, and no bracket-miscount retry. |
| D62 | `mps_mcp_list_node_intentions` (`JetBrainsMPSIntentionsMcpToolset.kt:105`) | Always returns a temp-file path via `saveToTempFileResult`, even for a 30 B result. It is the last reader without `maxInlineBytes`, so every intention listing costs a second Read or Bash call. | round 15: `S2-opus-1:65-66, :72-75` (30 B file), `S2-sonnet-1:114-115` (533 B) | **fixed 2026-09-25** (`b727f965586a`), not yet measured. `list_node_intentions` takes `maxInlineBytes` (default 20000) and returns through `finalizeResult(entries.toString(), maxInlineBytes)`, so `data` is the entry array inline and a temp-file path only above the threshold. No `saveToTempFileResult` caller outside `finalizeResult` remains. The tool description, `analysis-tools/intentions-and-quick-fixes.md`, `mcp-tools-index.md` and the "Read tools inline small results" list in `analysis-tools/overview.md` say so in all three catalog copies, and rule 8 of `test_scenarios/smoke-test-intentions.md` no longer sends the agent to a temp file that is not there. Integration test `list honours maxInlineBytes on both sides of the threshold`; `McpToolsIntegrationTestSuite` 1024/1024 on the change. Re-measure on the next S2 run: no Read or Bash of an intentions temp file. |
| D69 | `mps-mcp-workflow/references/bulk-creation.md`; `mps-node-editing/SKILL.md:62` | The bulk route both S3 workers read never names `table_to_bulk_insert.py` (only `mps-node-editing/SKILL.md:154` does), so both wrote their own CSV→blueprint script. "dryRun first if the blueprint is large" produced 65 false "did not resolve" warnings (17 KB) for intra-batch names, which the real insert resolved. | round 15: `S3-opus-1:10, :11 → :12, :16-18`; `S3-sonnet-1:15-16`; shipped scripts 0 uses in round 15 (4 in round 13) | **fixed 2026-09-25**, not yet measured. **D**: `bulk-creation.md` has a new "From a table" section that routes to `mps-node-editing`'s `scripts/table_to_bulk_insert.py`, using the same-origin companion wording. Its name-placeholder list now says a `dryRun` cannot see names inside the batch, and that `fixReferences.stillBroken` after the real insert is the check. `mps-node-editing/SKILL.md` step 4, the script's usage block and its `--help` say the same and no longer prescribe dry-run-then-write. **P-off**: `table_to_bulk_insert.py TABLE SPEC --verify <dump>` compares a `get_project_structure(startingPoint=<model>, includeNodes=true, nodeDepth=1)` dump with the table. It pairs rows with roots by `nameColumn` and checks every property, child and reference target the spec produces. It prints one line per difference plus a `{rows, matched, mismatched, missing, differences, extraRoots}` summary, and exits 1 on any difference. A dump that is too shallow or of the wrong kind exits 3. It reads the dump through `mps_dump.py`, as `concept_shape.py` does, and `--list-tools` adds `mps_mcp_get_project_structure`. The wording is generic (lesson 19), and the three catalog copies are identical. Integration test `table_to_bulk_insert --verify reports only the differences between a model dump and the table` in `SkillScriptsDriftTest` passed in `McpToolsIntegrationTestSuite` (1025/1025, 0 failed, 2026-09-25). Also checked live against MPS 261.25134: on a scratch copy of round 15's `S3-opus-1` project, a `get_project_structure(startingPoint=<model>, includeNodes=true, nodeDepth=1)` dump of the samples model verified 40/40 against `recipes.csv`, 5 seeded CSV changes were each reported on the right row (exit 1), and a `nodeDepth=0` dump was rejected (exit 3). Re-measure on the next S3 run: no self-written CSV→blueprint or CSV-vs-dump script, no bulk `dryRun`, and at most one verification Bash call. |
