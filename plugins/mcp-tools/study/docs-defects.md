# Documentation defects found during the skill-script automation study

Kept separately from the hotspot work (study section 8) so fixes are not blocked on scripting.
Each entry: where, what is wrong, evidence (run id + step, or commit), status.

| # | Where | Defect | Evidence | Status |
|---|-------|--------|----------|--------|
| D1 | `mps-mcp-workflow/references/mcp-tools-index.md` | Study 1.2(4) claims the index omits `search_root_node_by_name`, `parse_java_and_insert`, `create_run_configuration`. | All three present as of commit fc28c2636dce (verified 2026-09-15). | already fixed |
| D2 | `test_scenarios/StateChartScenario.md` | Contains reporting requirements ("keep ongoing notes", "final report"), which study 6.2 forbids in worker prompts. | file header | not a worker prompt; use as fixture builder only |
| D3 | tool descriptions / `mps-node-editing` (file-path `childJson`) | On macOS the worker wrote its blueprint to `/tmp/...` and got `Input file path '/tmp/…' is not inside the system temp directory`. Neither the tool description nor the skill says the file must be under `java.io.tmpdir` (`$TMPDIR`, `/var/folders/...` on macOS), and the error does not name the accepted directory. | S1-opus-1 (first error result, ~step 40) | provisional — confirm in transcript, then fix wording |
| D4 | `mps_mcp_get_concept_details` (and likely other name-resolving tools) | `unresolved[].suggestions` for a plain concept name are taken from the shared module repository, so with a same-named language in another open project the suggestions point into that other project (conceptReference with the foreign language UUID). Not a docs defect strictly — a tool-behaviour issue: suggestions should be scoped to the selected project or be labelled with their project. Note this entry names only the suggestion-scoping issue; round 3's defect table used the label `D18/D4` for missing `projectPath`, which is D18 alone — D4 is not an alias of it. | S1-sonnet-1, get_concept_details result containing `c:d10ad89a-…` (opus project) | pending verification — the scoping reading looks already addressed (`suggestForUnresolvedConceptRef` ranks over `languagesInProject(...)`, `JetBrainsMPSLanguageMcpToolset.kt:704`, `:719-722`; `search_concepts` drops other projects' languages at `:269-272`); confirm in a two-project session, then close |
| D5 | `mps_mcp_print_node` JSON output / `analysis-tools.md` | An enum property holding the enumeration's *default* literal is printed as `"value": ""` (MPS stores nothing for the default). The evaluator initially read 13 recipes as missing `difficulty`; the plain-text projection shows `easy`. The output schema doc should state that an empty enum value means "default literal" (or the printer should emit the default's name with a flag). | S3-opus-1 evaluation; fixture recipe Crepes shows the same | open |
| D6 | `mps_mcp_create_module` result / `mps-mcp-workflow` | Both S1 workers tried to create `mcp.study.recipes.structure` after `create_module` had already created the aspect models (`No suitable model root found … to create model`). The result should list the models it created; the skill should say a new language comes with structure/editor/… models. | S1-opus-1, S1-sonnet-1 first error envelope | open |
| D7 | `mps_mcp_check_root_node_problems` (model reference) | Workers re-check every root after a clean model-level result (S3-opus-1: 50 calls). The envelope should state coverage, e.g. `rootsChecked: 48`, so the model-level answer is visibly exhaustive. | S3-opus-1 steps ~35–95 | open — tool envelope |
| D8 | `mps_mcp_get_project_structure`, `mps_mcp_print_node` | Always return a temp-file path, even for tiny payloads, forcing a second call to read it (32/23/25/4 envelopes per run). Inline small results; add `fields`/`summary` options for the large ones. | all runs; chains #1, #6, #9 | open — tool behaviour |
| D9 | `mps_mcp_print_node` `format` / `analysis-tools.md` | Allowed literals (`JSON`, `HTML`, `PLAIN TEXT`) are not listed; the space-containing literal is unguessable ("text" rejected). | S1-sonnet-1:110-111 | open |
| D10 | `mps_mcp_update_node` `childJson` vs `insert_root_node_from_json` | Top-level array accepted by the insert tool but rejected by `update_node` (`Expected JsonObject but was JsonArray`); asymmetry undocumented, forced 3 single-child calls. | S1-sonnet-1:129-132 | open |
| D11 | file-path `childJson` error text | "Write the JSON to a temp file (e.g. via File.createTempFile)" is a Java hint given to a shell agent; should name `$TMPDIR`. Worker wrote itself a memory note about it. | S1-opus-1:16-18, :173 | open (with D3) |
| D12 | `get_project_structure includeNodes` dump | Properties holding their default value are omitted from the dump, so verification scripts report false "missing" values (`KeyError: 'difficulty'`). Same root cause as D5. | S3-opus-1:28-33, :97 | open |
| D13 | `alter_structure UPDATE_CONCEPT_REFERENCE`, `mps-aspect-structure-concepts` | `multiple` is read and honoured for children but never read for references (hard-coded single-valued, cardinality `_0__1`/`_1`); unknown keys in `parameters` are dropped silently, so `multiple:true` on a reference returns `ok:true` and quietly yields a 0..1 link. No shipped skill states that MPS reference links are always single-valued, nor names the smart-reference wrapper idiom for `[0..n]` references. | round 2: S1-sonnet-1:16-57 (~41 tool calls to rediscover, incl. a scratch-concept experiment at `:42`); source `JetBrainsMPSLanguageStructureMcpToolset.kt:291-296` and ~1204; `structure-operation-api.md:204` vs `:209-220` | open — docs (N1) + reject the ignored key (N2) |
| D14 | `mps_mcp_get_concept_details` | `conceptReference` — the parameter name `print_node`/`scaffold_editor` use — is not recognised here. Round 2's reading ("the error names neither the offending key nor the correct one") no longer holds: the message names the plural `conceptRefs`/`languageRefs` and calls out the singular spellings explicitly (`JetBrainsMPSLanguageMcpToolset.kt:104-108`), and `search_concepts` likewise answers `"searchTexts is required: …"` (`:240`). Round 3's cost was therefore **not** an unnamed key — the agent fetched the schema anyway. | round 2: S1-sonnet-1:95; round 3: S1-sonnet-1:56-58 (`query` vs `searchTexts`) | open — the message needs a copy-pasteable retry line (M5b/M6a), not an alias parameter, which would grow the per-turn schema |
| D15 | `mps_mcp_update_node SET/CHILD` | Docs say `childJson = null` deletes the child, but the string `"null"` gives "Input is neither a valid JSON object/array nor an existing file path: 'null'" without stating how to express the null. | round 2: S1-sonnet-1:154 | open |
| D16 | shipped catalog vs installed copies | The 2026-09 treatment updated `plugins/mcp-tools/resources/**` only; the checkout's own `.agents/skills/` and `.claude/skills/` (42 differing entries) and every already-initialised project still serve pre-treatment docs — no `scripts/`, no `rootsChecked`, no jump tables. Refreshing the study's golden project was a precondition for round 2. | `diff -rq` of resources vs both trees, 2026-09-16 | fixed 2026-09-17 — catalog propagated to the checkout (N6, verified by `diff -rq`); the per-run install (D19) stops it recurring in the study |
| D17 | `study/scripts/analyze_runs.py` | A run that delegates to a subagent emits one `result` event per session; the analyzer kept the last, reporting the subagent's numbers (S1-sonnet-1: 13 turns / 92 s instead of 115 / 952). | round 2: two `result` events in S1-sonnet-1-worker.jsonl | fixed 2026-09-16 (keep the event with the most turns; baseline re-run is byte-identical) |
| D18 | `mps_mcp_list_open_projects` (round 2 labelled this "D4, restated"; it is not — see D4) | The discovery call itself refuses to run without `projectPath`, even with exactly one project open, costing a wasted turn per fresh session — and `initialize_project_for_agents` demands it although CLAUDE.md and the tool description say not to pass it. | round 2: S3-sonnet-1:10, S3-sonnet-2:6; round 3: S1-sonnet-1:11-13 (2 of 5 error envelopes, 2 wasted turns); observer hit it twice | partly fixed 2026-09-17 — M3a reconciled the contradictory instructions: one unconditional `projectPath` Critical Directive in `mps-mcp-workflow/SKILL.md` carrying the recovery (the rejection message lists the open projects), referenced from `AGENTS_template.md` and the two repo-root guides, and the "do not pass `projectPath` to `initialize_project_for_agents`" line removed. The pre-dispatch rejection itself is platform routing, filed separately as M3b (N3) |

| D19 | `study/fixtures/*.tar.gz` + `study/scripts/run_worker.sh` | The harness half of D16: a fixture tarball carried `.claude/skills`, `.agents/skills`, `AGENTS.md` and `CLAUDE.md`, so every run of every later round measured the catalog as it was when the tarball was built, while `promptSha256`/`inventorySha256` pinned only the scenario and the tool surface — a catalog change was invisible in the evidence. | round 2 precondition (manual refresh); `FIXTURE_SKILLS_REFRESH_PLAN.md` | fixed 2026-09-17 — fixtures exclude the doc surface; `run_worker.sh` installs the live catalog per run via `scripts/install_skills.py` and records `skillsSha256` |
| D20 | `mps_mcp_get_concept_details` | Passing `conceptRefs` as fully-qualified concept names (`mcp.study.recipes.structure.Recipe`) — the exact strings the tool itself reports as `qualifiedName` — raises an unhandled `Index -1 out of bounds for length 0` instead of resolving them or returning an `ok:false` envelope naming the expected reference form. `languageRefs` works. | round 3: S1-sonnet-1 evaluation, 2026-09-17 | open — accept the FQN form or fail with a message |
| D21 | `study/scenarios/S1/done_criteria.md` criterion 2 | Names `query_structure` / `search_concepts` for "list the concepts of a language", which neither tool can do: `query_structure` has no list-concepts operation and `search_concepts` hard-requires `searchTexts`. The workable route is `get_project_structure(startingPoint=<structure model>)` + `get_concept_details(languageRefs=…)`. | round 3: S1-sonnet-1 evaluation | fixed 2026-09-17 — M9 rewrote criterion 2 around that route and, since an enumeration has no lookup by qualified name (D22), routes the enum check through the roots dump: `print_node(deep=true)` or `GET_ENUMERATION_LITERALS(enumerationRef=<node ref>)`. The worker prompt is untouched (`promptSha256`) |
| D22 | `mps_mcp_get_concept_details` | An enumeration cannot be resolved by qualified name: `mcp.study.recipes.structure.Difficulty` returns `NOT_FOUND` and suggests the unrelated `…structure.Recipe`. The declared default is reachable only through an owning concept's `properties[].enumerationDefault` — which is exactly the lookup the D5 "is this enum value missing?" pitfall forces on every reader. Re-scoped 2026-09-17: the root cause is structural, so `get_concept_details` can **never** resolve an enumeration — concept resolution requires `root.concept.isSubConceptOf(concept_AbstractConceptDeclaration)` (`AbstractOps.kt:2673`, `:2691`) and `EnumerationDeclaration` is not one. The route the error should name, `query_structure GET_ENUMERATION_LITERALS`, resolves `enumerationRef` through `resolveNodeReferencePreferringProject` (`JetBrainsMPSLanguageStructureMcpToolset.kt:1102`) and so needs a **node reference** — it rejects the qualified name too. A qualified enumeration name has no working lookup anywhere in the tool surface. | round 3: S3-sonnet-1 evaluation, 2026-09-17 | open — M5c: route the error to `GET_ENUMERATION_LITERALS` and let `enumerationRef` accept a qualified name. Today's working route (roots dump → node reference → `GET_ENUMERATION_LITERALS` or `print_node`) is now recorded in S1 `done_criteria.md` (D21) |
| D23 | `mps_mcp_print_node` (JSON format) + `analysis-tools.md` | Re-worded 2026-09-17: the round-3 premise ("no depth parameter") is **wrong** — `deep=true` already inlines all descendants (`JetBrainsMPSNodeMcpToolset.kt:815`, dispatched `:822`/`:829`, documented at `analysis-tools.md:57`), and `format:"PLAIN TEXT"` renders the whole root in one call (~10x cheaper for verification). The actual defect is routing: the docs did not name `deep=true` / `PLAIN TEXT` as *the* verification path, so the evaluator walked the children at one call per step while JSON stayed at its shallow default. | round 3: S3-sonnet-1 evaluation | fixed 2026-09-17 — M4 added the "Verifying a whole root costs one call" paragraph to `analysis-tools.md` plus cross-references from the tool-inventory bullet and the `check_root_node_problems` section. Docs only; no server change needed |
| D24 | `mps_mcp_print_node` PLAIN TEXT + `analysis-tools.md` | An EMPTY `0..n` role renders as `see also: << ... >>`, which a naive reader parses as a one-element list. The empty-collection placeholder is undocumented. | round 3: S3-sonnet-1 evaluation | fixed 2026-09-17 — M4 documented it next to the verification recipe in `analysis-tools.md`: an editor placeholder means the role is *empty*, and counts must come from the JSON form, never from the projection |
| D25 | temp-file envelopes | The temp file named by `data` contains a COMPLETE `{"ok":…,"data":…}` envelope rather than the bare payload, so a caller that splices the file in as `data` gets `data.data`. Undocumented; cost one debugging round trip in the observer's own CLI. Scoped 2026-09-17: `analysis-tools.md:55` already stated the envelope shape for `print_node` and `:60` that the JSON mutation tools accept either form — only the generic "Read tools inline small results" paragraph at `:7` was silent. | round 3: observer, `get_project_structure` with 44 roots | fixed 2026-09-17 — M7 generalised the envelope shape to all read tools at `analysis-tools.md:7`. The payload stays an envelope: `saveToTempFile` writes `okJson(json)` (`AbstractOps.kt:3246-3247`) for every tool, so emitting the bare payload would change what every existing consumer parses for no measured gain |
