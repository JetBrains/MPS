# Documentation defects found during the skill-script automation study

Kept separately from the hotspot work (study section 8) so fixes are not blocked on scripting.
Each entry: where, what is wrong, evidence (run id + step, or commit), status.

Closed, fixed, rejected, and out-of-scope entries were moved to [`docs-defects-archive.md`](docs-defects-archive.md) on 2026-09-22, and D43–D47 on 2026-09-24. This file holds only issues still to be handled.

| id | where | what is wrong | evidence | status |
|---|-------|--------------|----------|--------|
| D48 | no `mps_mcp_*` tool to execute a pending migration; `mps_mcp_reload_all` | With D46 fixed, S4 gets past the version bump cleanly (`languageVersion` read/write both verified working) but still fails end-to-end: there is no MCP tool that runs a pending `MigrationScript` against a consumer model headlessly. The worker's only lever was `mps_mcp_reload_all`, which — because the consumer module's `usedLanguages` stamp (0) now trails the bumped `languageVersion` (1) — makes MPS itself pop its own migration-assistant `DialogWrapper` (`jetbrains.mps.ide.migration.MigrationTrigger.runMigration`, confirmed via `jstack`: `AWT-EventQueue-0` blocked in `Dialog.show`). Every later `mps_mcp_*` call then times out `MODAL_BLOCKED` for the rest of the run. The worker correctly stopped and asked for help rather than forcing anything, but its transcript ended there — task-level result 2/4 criteria (structure + version bump pass; migration-applied + zero-errors fail) even though the fix under test worked. | `S4-sonnet-1` (round 9 re-run, 2026-09-23): server calls 52-56 all `MODAL_BLOCKED`; thread dump `/tmp/mps-threaddump-s4.txt:188-237` | open — **S**: either a headless "run pending migrations on module X" tool (apply without the confirmation UI, mirroring what `MigrationTrigger` does programmatically), or have `reload_all` (or a new explicit trigger) surface `MODAL_BLOCKED`-avoidance by running migrations non-interactively when invoked over MCP. **D**: `mps-aspect-migrations` should say plainly that no tool runs a migration yet and that touching `reload_all` (or anything that re-checks consumer versions) on a version-bumped-but-unmigrated project risks this modal |
| D49 | `mps_mcp_alter_nodes` `MOVE_CHILD` | Missing parameters are disclosed one at a time, unlike every other operation checked in round 10 and unlike the unified `AbstractOps.rejectMissingParameters` path D43 describes. Each error names only the next missing key, so learning both required parameters costs two sequential retries. Round 10's reading: `MOVE_CHILD`'s own parameter validation is not routed through that helper. | round 10: `S2-sonnet-1:34` (missing `nodeReference`) → `:35` (now missing `childRole`) → `:36` succeeds. Round 11 §5: still unfiled. | open — **S**: one error should list every missing key (`missingParameters`), the same helper the rest of the round's evidence shows working. Round 10 assigned this the next id. |
| D50 | split reference dirs and their owning `SKILL.md` files (`mps-aspect-constraints`, `mps-aspect-structure-concepts`, `mps-model-manipulation`, `mps-aspect-generator`) | D44's payload split landed — bytes are back near the 2026-09 baseline, and the `grep -n '^## '` index-rebuild is gone — but the turn-count half did not. Removing the in-file jump tables left no per-job index the agent reads, so navigation moved from grepping one big file to full-text `grep` across the new per-section directories, and in round 11 across the whole catalog. D44's own round-10 self-check (zero `grep` against `references/` in S1/S2/S4; S1 skill-navigation calls ≤ 25, was 45) failed: S1 stayed at 43 reads, and S2 was worse than every prior measurement (31 reads / 140,876 B). Round 11 shows the same shape in a skill round 10 did not sample. | round 10 §3.3: `S1-sonnet-1` repeated `grep -rl` then `grep -n -B3 -A15` (7 times) under `mps-aspect-constraints/references/` and `mps-model-manipulation/references/`; `S2-sonnet-1` 31 reads / 140,876 B. Round 11 §3.3: `S6-sonnet-1` `grep -rn` and `grep -rln` on `mps-aspect-generator/references`, plus one catalog-wide `grep -rln` under `.claude/skills/`. | open — **D**: a directory-level index (one line per section file, keyed by symptom/task) in each split reference's owning `SKILL.md`, including `mps-aspect-generator`. Do not treat archived D44's "measure in round 10" line as passed. Re-measure after the index exists. |
| D51 | `mps-model-manipulation`, `mps-quotations` (JSON blueprint / BaseLanguage AST shape) | A collection literal built directly as a `ReturnStatement.expression` is rejected: that role wants an `Expression`, and an `ArrayCreatorWithInitializer` is not one until wrapped in a `GenericNewExpression`. D43's near-miss vocabulary covers parameter names, not AST shape, so recovery is ad-hoc discovery — about 5 extra calls. Neither skill documents the wrapper. | round 11: `S6-sonnet-1:57` rejected; `:58`–`:59` `get_concept_details` on the rejected concept and siblings; `:60`–`:62` `ToolSearch` + `search_concepts` + `get_concept_details` to find `GenericNewExpression`; `:63` succeeds. | open — **D**: state that an array or collection literal needs a `GenericNewExpression` wrapper to be used as an `Expression`. |
| D52 | `mps-model-manipulation`, `mps-quotations` (`SPropertyAccess.property`) | A JSON-blueprint `SPropertyAccess.property` reference pointed at the enclosing `ConceptDeclaration` (`Recipe`) instead of the `PropertyDeclaration` (`name`). Same concept-assignability class as D51, different site; about 4 extra calls on top of the direct-JSON-too-large redirect that preceded it. Undocumented in both skills. | round 11: `S6-sonnet-1:109`–`:110` temp-file redirect; `:111` rejected; `:112`–`:113` `print_node` on the wrong and the right target; `:114` edit; `:115` succeeds. | open — **D**: state that `SPropertyAccess.property` must reference the `PropertyDeclaration`, not its owning concept. |
| D53 | `mps_mcp_create_run_configuration`; `mps-run-configurations` `SKILL.md` | The tool accepts only a single root node, so an `@tests` model with 4 roots required 4 separate "JUnit Tests" configurations — there is no model-level or bulk target. None of the four is written to `.mps/workspace.xml`; they exist only for the running IDE session and are lost on restart. Both facts are absent from the tool description and the skill. No retry cost (the worker adapted immediately), but a later agent in the same session, or a human reopening the project, would find the tests passing with no persisted evidence on disk. | round 11: `S7-sonnet-1` evaluator report (first measurement of S7). | open — **D**: document the one-root scope and that created configs are session-only, not written to `workspace.xml`. **S** only if a model-level target or workspace persistence is actually wanted; neither exists today. |
| D54 | console response printing; `mps-console`; `get_console_history` | A query that returns a sequence executes correctly and is recorded in console history in the right order, but the printed response is a `NodeWithClosure` whose text is only an item-count summary (`"3 nodes"`), not the item values. The done-criteria check that the printed items include all 3 Ingredient names could only be partially verified: the count matches, the names do not appear. An agent or a human that needs the values back through the read-only console path has to shape the query (iterate and `#print` per item) rather than rely on `get_console_history`. | round 11: `S9-sonnet-1` (first measurement of S9); `#instances(Ingredient)` and its filtered variant, verified live before the project closed. | open — **D**: say this response-printing behavior in `mps-console`, including the per-item `#print` workaround. |
| D55 | `mps_mcp_query_structure` `LIST_CONCEPT_ASPECTS` | This operation uniquely takes a singular `conceptRef`. Sibling tools converged on plural `conceptRefs` after D43. The near-miss message names the fix and the worker self-corrected in 1 retry, so the turn cost is low, but the naming surface D43 was meant to unify still has this one holdout. | round 11: `S8-sonnet-1:48`→`:49`. | open — **S**: accept plural `conceptRefs` (or both), matching the post-D43 siblings. **D** if the singular key is intentional: call that exception out next to the other reference-parameter rules. |
| D56 | `mps_mcp_get_concept_details` near-miss list; `mps_mcp_print_node` parameter name | `concept` / `conceptName` are outside the near-miss vocabulary, so the generic "no concepts nor languages provided" message fires and the worker repeats the same wrong key on a second concept before switching to `conceptRefs`. That is now a third confirmed instance (round 8 S2, round 10 S1/S2, round 11 S6). `print_node` `node` (should be `nodeReference`) showed the same two-in-a-row miss on two different node references; not previously observed on that tool. Not the same as `apply_intention` `targetNode`, where the D43 message worked in one retry (`S7-sonnet-1:88`→`:89`), and not `search_concepts` `query`, whose error already names `searchTexts`. | round 10: `S2-sonnet-1:118,:119,:120` (`conceptName` twice, then `conceptRefs`); `S1-sonnet-1:17`→`:18` is the covered singular `conceptRef` case (1 retry) and is not this gap. Round 11: `S6-sonnet-1:10,:11,:14` (`concept` twice, then `conceptRefs`); `S6-sonnet-1:12,:13,:15` (`node` twice, then `nodeReference`). | open — **S**: add `concept` and `conceptName` to the `get_concept_details` near-miss list, with a copy-pasteable retry the way D43 did for `conceptRef`. Confirm whether `print_node`'s rejection of `node` already names `nodeReference`; if it does not, give it the same treatment. |

D39 and everything before it were archived on 2026-09-22; D43–D47 on 2026-09-24.
D49–D56 were filed on 2026-09-24 from `HOTSPOT_REPORT_round10.md` and `HOTSPOT_REPORT_round11.md`. Those rounds were measurement only; no remedy was chosen there.

Not opened from those rounds:

- D34 is confirmed fixed (round 11 §3.6: S6 generated correctly packaged Java with zero package-related errors). Already in the archive.
- D3 did not reproduce (round 11 §3.8: `S6-sonnet-1:110`→`:111` wrote `/tmp/describe2_blueprint.json` and `update_node` read it back with no temp-directory rejection; the only error there was D52). One data point. The archive still says fixed 2026-09-15, while round 6 recorded the rejection open in practice. Reconcile those two records before reopening D3.
- `alter_nodes MAKE` with top-level `targets` / `rebuild` instead of nested `parameters` is accepted D29 (kept strict on purpose). Unchanged 1-retry cost: round 11 `S6-sonnet-1:117`→`:118`.
- `search_concepts` `query` still costs a retry even though the error already names `searchTexts` (round 10 `S2-sonnet-1:121`–`:123`, two misses; round 11 `S6-sonnet-1:99`→`:100`, one). That is the model ignoring a correct message, not a new docs defect.
- Hotspots that stayed fixed (#1 temp-file envelope, #2 per-root re-validation, #5 bulk-insert summary, and D34) and harness-level ToolSearch fetches (hotspot #8) are not defects to handle here.

The three harness defects found in round 8 (D40 user-skill guard, D41 pre-dispatch
misclassification, D42 no scripted way to enable the call log) were **fixed in the same commit** and
are recorded in `HOTSPOT_REPORT_round8.md` §6 and study lessons 32–34, not here.
