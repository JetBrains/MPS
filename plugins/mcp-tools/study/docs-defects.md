# Documentation defects found during the skill-script automation study

Kept separately from the hotspot work (study section 8) so fixes are not blocked on scripting.
Each entry: where, what is wrong, evidence (run id + step, or commit), status.

Closed, fixed, rejected, and out-of-scope entries were moved to [`docs-defects-archive.md`](docs-defects-archive.md) on 2026-09-22, and D43–D47 on 2026-09-24; D49, D51, D52, D54, D55, D56, D57, D58, D59, D60, D61, D62 (`b727f965586a`), and D69 (`edbc0a14a9ec`) on 2026-09-25. This file holds only issues still to be handled (D62–D69 were filed 2026-09-25 from round 15; D62 and D69 were each fixed and archived in their own commit. D70–D79 and A2 were filed 2026-09-25 from round 16).

| id | where | what is wrong | evidence | status |
|---|-------|--------------|----------|--------|
| D48 | no `mps_mcp_*` tool to execute a pending migration; `mps_mcp_reload_all` | With D46 fixed, S4 gets past the version bump cleanly (`languageVersion` read/write both verified working) but still fails end-to-end: there is no MCP tool that runs a pending `MigrationScript` against a consumer model headlessly. The worker's only lever was `mps_mcp_reload_all`, which — because the consumer module's `usedLanguages` stamp (0) now trails the bumped `languageVersion` (1) — makes MPS itself pop its own migration-assistant `DialogWrapper` (`jetbrains.mps.ide.migration.MigrationTrigger.runMigration`, confirmed via `jstack`: `AWT-EventQueue-0` blocked in `Dialog.show`). Every later `mps_mcp_*` call then times out `MODAL_BLOCKED` for the rest of the run. The worker correctly stopped and asked for help rather than forcing anything, but its transcript ended there — task-level result 2/4 criteria (structure + version bump pass; migration-applied + zero-errors fail) even though the fix under test worked. | `S4-sonnet-1` (round 9 re-run, 2026-09-23): server calls 52-56 all `MODAL_BLOCKED`; thread dump `/tmp/mps-threaddump-s4.txt:188-237` | open — **S**: either a headless "run pending migrations on module X" tool (apply without the confirmation UI, mirroring what `MigrationTrigger` does programmatically), or have `reload_all` (or a new explicit trigger) surface `MODAL_BLOCKED`-avoidance by running migrations non-interactively when invoked over MCP. **D**: `mps-aspect-migrations` should say plainly that no tool runs a migration yet and that touching `reload_all` (or anything that re-checks consumer versions) on a version-bumped-but-unmigrated project risks this modal |
| D50 | split reference dirs and their owning `SKILL.md` files (`mps-aspect-constraints`, `mps-aspect-structure-concepts`, `mps-model-manipulation`, `mps-aspect-generator`) | D44's payload split landed — bytes are back near the 2026-09 baseline, and the `grep -n '^## '` index-rebuild is gone — but the turn-count half did not. Removing the in-file jump tables left no per-job index the agent reads, so navigation moved from grepping one big file to full-text `grep` across the new per-section directories, and in round 11 across the whole catalog. D44's own round-10 self-check (zero `grep` against `references/` in S1/S2/S4; S1 skill-navigation calls ≤ 25, was 45) failed: S1 stayed at 43 reads, and S2 was worse than every prior measurement (31 reads / 140,876 B). Round 11 shows the same shape in a skill round 10 did not sample. | round 10 §3.3: `S1-sonnet-1` repeated `grep -rl` then `grep -n -B3 -A15` (7 times) under `mps-aspect-constraints/references/` and `mps-model-manipulation/references/`; `S2-sonnet-1` 31 reads / 140,876 B. Round 11 §3.3: `S6-sonnet-1` `grep -rn` and `grep -rln` on `mps-aspect-generator/references`, plus one catalog-wide `grep -rln` under `.claude/skills/`. | open — **D**: a directory-level index (one line per section file, keyed by symptom/task) in each split reference's owning `SKILL.md`, including `mps-aspect-generator`. Do not treat archived D44's "measure in round 10" line as passed. Re-measure after the index exists. |
| D53 | `mps_mcp_create_run_configuration`; `mps-run-configurations` `SKILL.md` | The tool accepts only a single root node, so an `@tests` model with 4 roots required 4 separate "JUnit Tests" configurations — there is no model-level or bulk target. None of the four is written to `.mps/workspace.xml`; they exist only for the running IDE session and are lost on restart. Both facts are absent from the tool description and the skill. No retry cost (the worker adapted immediately), but a later agent in the same session, or a human reopening the project, would find the tests passing with no persisted evidence on disk. | round 11: `S7-sonnet-1` evaluator report (first measurement of S7). | open — **planned** in [`../docs/d53-run-configuration-scope-plan.md`](../docs/d53-run-configuration-scope-plan.md) (all decisions made 2026-09-25; nothing implemented). **Correction:** the persistence half does not hold. The tool registers a permanent config, and IntelliJ writes it at the next settings save: the S7 project's `workspace.xml` holds all four configs, written 5 s after the evaluator's report, which checked the file before MPS had saved it. So do **not** document configs as session-only. The one-root half holds. Remedy **S**: one call names any mix of test roots (`nodeReference`) and models (`modelReference`) and gets one JUnit config and one launch; a model reference no longer gets a misleading `NOT_FOUND`. |
| D63 | `mps_mcp_check_root_node_problems` (`JetBrainsMPSNodeMcpToolset.kt:904-907`) | A module reference is rejected (NOT_FOUND). The rejection always ends "This tool has no modelReference parameter; retry with nodeReference set to the value you passed as modelReference", even when the caller passed `nodeReference`. The worker then checks each aspect model one by one; 4 of those 5 models had not changed since their last clean check. | round 15: `S1-opus-1:116` → `:117-121`; round 13: `S1-opus-1:80-82, :96-97` (the same sweep) | open — **planned** in [`../docs/d63-module-scope-problem-check-plan.md`](../docs/d63-module-scope-problem-check-plan.md) (analysis done 2026-09-25, both claims hold; decisions D1–D6 await sign-off; nothing implemented). **S**: accept a module reference (check every owned model, return `[{model, rootsChecked, errors, warnings}]`); emit the `modelReference` sentence only when that key was passed |
| D64 | `mps-aspect-constraints/references/referent-constraints/cross-model-filtered-scope.md:22`, `mps-aspect-constraints/SKILL.md` routing table; `get_project_structure startingPoint` | The "no self-reference" scope needs `ListScope.forNamedElements`. The file workers are routed to names it without a persistent ref; the refs are only in `composite-scope-addscope.md` / `scope-helpers.md`, which neither S1 worker read. The lookup via `startingPoint=jetbrains.mps.scope` then fails with "non-project model … filtered out" and is retried with `includeStubModules`, at the same step, in three rounds. | round 15: `S1-sonnet-1:47-50` (4 calls, 22.6 KB), `S1-opus-1:52-55` (4 Bash); `S1-sonnet-1:47→48` also at round 10 `:48` and round 13 `:52` | open — **D**: add the method ref to `cross-model-filtered-scope.md:22` and a routing row "exclude the referrer itself" in the SKILL.md. **S** (optional): an explicit non-project `startingPoint` returns the model with a warning instead of NOT_FOUND |
| D65 | `mps-aspect-constraints/SKILL.md:60` | Step 2 says to add `jetbrains.mps.lang.constraints` (and smodel / scopes / behavior / baseLanguage) to the used languages. A constraints model created by `create_module` already uses the `jetbrains.mps.devkit.aspect.constraints` devkit, so each add is a no-op answered `providedByDevKit`. | round 15: `S1-opus-1:57-59` (3 calls) | open — **D**: say the aspect devkit provides these languages; only the model imports (`jetbrains.mps.scope`, `jetbrains.mps.lang.core.behavior`) are needed |
| D66 | `mps-aspect-typesystem/references/non-typesystem-checking.md` (routed from `SKILL.md:57` as "only") | 34 lines with fields, a DSL example and FQNs, but no JSON blueprint. The worker searches 6 more files for the shape it then inserts cleanly. | round 15: `S1-sonnet-1:86-92` → `:93`; `S1-opus-1:71-72` | open — **D**: add a "warning on a property value" blueprint (`ConceptReference` applicable node, `ApplicableNodeReference`, `WarningStatement`) taken from `S1-sonnet-1:93` |
| D67 | `mps_mcp_get_concept_details detail:"shape"` (`JetBrainsMPSLanguageMcpToolset.kt:688-697`) | Link and property entries carry name / targetConcept / cardinality, but not the declaration node ref. Editor cells and `SLinkAccess` need that ref, so the worker makes a second `detail:"full"` or `print_node` call. | round 15: `S1-opus-1:26-28`, `S2-sonnet-1:20→40`, `S2-opus-1:17→27` | open — **S**: add `sourceNode` (persistent ref of the Link/PropertyDeclaration) per feature |
| D68 | `mps_mcp_parse_java_and_insert` inside a `ConceptFunction` body; `java-parser-capabilities/unsupported-and-workarounds.md` | `propertyValue` / `node` / `contextNode` parse as a plain `VariableReference`, so both S2 workers replaced them with `ConstraintsFunctionParameter_*` by hand. The invalid statement `propertyValue != null && …;` came back `ok:true` as an **assignment** `propertyValue = (null && …)`. | round 15: `S2-opus-1:41-45` (the wrong parse at `:41`, noticed at `:42`), `S2-sonnet-1:72-77` | open — **S**: bind the function's implicit parameters as `this` is already bound for behavior, and reject an expression statement that is not a statement expression. **D** fallback: a row for `propertyValue` in the workarounds table |
| D70 | `mps_mcp_print_node` JSON (and `get_project_structure` node dumps); `mps-dsl-memory/SKILL.md:26`, `mps-language-analysis/SKILL.md:31` | A default-valued **boolean or integer** property is omitted, so a `BooleanConstant` holding `false` prints as `"properties": []`. Enums already print the default with `"isDefault": true` (D5/D12). The omission rule is stated only in `mps-mcp-workflow/references/analysis-tools/print-node-output.md:48`, which the worker never read. S8-sonnet read the deep JSON of `State_Constraints` and wrote a skill that inverts both property validators (`== false` recorded as `== true`, "they don't enforce the invariant"). Round 12's sonnet read the same roots as `PLAIN TEXT` and got them right. | round 16: `S8-sonnet-1:23` (86 KB Read; the three `BooleanConstant` nodes all have `"properties": []`, verified in the temp file), conclusion in the compaction summary after :38, written at :57/:60; `S8-sonnet-1.eval.md` | open — **D**: add "read aspect roots with `format: "PLAIN TEXT"`; deep JSON omits default values" after "Avoid dumping every root." in `mps-dsl-memory/SKILL.md:26`, mirrored at `mps-language-analysis/SKILL.md:31`. **S**: emit default boolean/int values like enums (`{"name":"value","value":"false","isDefault":true}`). |
| D71 | `mps-tests/references/editor-test-case.md:13`, `common-failures.md:9` | Both files list the `AnonymousCellAnnotation` properties (`caretPosition`, `isLastPosition`, `selectionStart/End`) but never say which ones the check compares. `CellReference.assertSelectionIsTheSame` compares only `cellId` + `selectionStart`/`selectionEnd` on the **result** annotation; `caretPosition`/`isLastPosition` only place the caret in `before`. | round 16: `S7-sonnet-1:133-149` (17 steps, 424 s, including `find /` into another checkout at :141-143, +3 re-runs); `S7-opus-1:53-57` (4 steps, same `expected:<0> but was:<10>` at `CellReference.java:61`) | open — **D**: append "the result annotation is compared by `cellId` + `selectionStart`/`selectionEnd` only; for a caret at the end of typed text set both to the text length" to `editor-test-case.md:13`, and map the assertion text to that fix in `common-failures.md:9`. |
| D72 | `mps-project-management/SKILL.md:51`; `references/create-empty-project.md:144-150` | The step list goes "create → then open the directory via the CLI activation workflow". Closing the open project first is only note 3 ("opening … **may** cause … the New Window / This Window prompt … You **can** close existing projects beforehand"). Activating while a project is open raises the modal prompt: the activation does not exit, and every later `mps_mcp_*` call, including the close, fails with `MODAL_BLOCKED`. | round 16: `S10-sonnet-1:24-33` (90 s activation timeout, `MODAL_BLOCKED` close after 20 s at :31, recovered with `force=true` at :32; ~190 s of a 332 s run). `S10-opus-1` and the 2026-09-24 `S10-sonnet-1` closed first, so the order is a coin flip | open — **D**: make "close the open project with `mps_mcp_close_project`" a numbered step before the open in both files, delete the soft note, and state the `MODAL_BLOCKED` consequence. |
| D73 | `mps-aspect-generator/SKILL.md:56` ("Path convention") | The generated Java package comes from the template class's `packageName` property. `parse_java_and_insert` sets it from a `package x.y;` line (D34 fix), but no skill says either fact. | round 16: `S6-opus-1:13-22` (10 steps searching MPS sources for the package mechanism); `S6-sonnet-1:38` shows the parser warning | open — **D**: one sentence in "Path convention". |
| D74 | `mps_mcp_print_node` `deep` parameter description (`JetBrainsMPSNodeMcpToolset.kt:963`); `mps-model-manipulation/SKILL.md:64` | Recurrence of archived D23, which was fixed in `analysis-tools.md` only. The skill line recommends "shallow `mps_mcp_print_node` before editing", and the always-loaded tool text never says that a whole method is one `deep=true` or `PLAIN TEXT` call. | round 16: `S6-sonnet-1:62-69`, `:72-75` (ten shallow prints walking Recipe_Behavior → method → … → `SLinkListAccess`); `S6-opus-1:54` did it in one call | open — **D**: reword the `deep` description and the skill line (text in `runs-r17/analysis/review.md` §F2). |
| D75 | `mps-language-analysis/references/concept-details.md:66` | Says `shape` "is everything needed to author a node", but `shape` omits `superConcept`/`superInterfaces` and per-feature `sourceNode` (D67). Sonnet walks the superconcept chain one `full` call per hop. `query_structure GET_ALL_SUPERCONCEPTS` / `IS_SUBCONCEPT_OF` answers it in one call but is documented only in `mps-language-inheritance/SKILL.md:37`. | round 16: `S7-sonnet-1:37-42` (5 serial hops), `:68-69`; `S6-sonnet-1:80-81` | open — **D**: append the omission and the `query_structure` route to `concept-details.md:66`. **S** (optional, with D67): `superConcepts` on each `shape` entry. |
| D76 | `mps-mcp-workflow/scripts/mps_dump.py` | Residual of D58: `roots` on a project or module dump that has no `rootNodes` prints `{"roots":0,"lines":0}` and exits 0, so the agent concludes the model is empty. There is no `models` view, and no `tree` view, which opus re-wrote 4×. Also, every sonnet opener passed `includeDependencies`, roughly doubling the listing. | round 16: `S6-sonnet-1:5-12`, `S7-sonnet-1:12-17`, `S8-sonnet-1:8-11`; tree rewrites at `S6-opus-1:28, :55`, `S8-opus-1:27, :33` | open — **P-off**: `models` and `tree [--depth N]` subcommands; exit 2 on `roots` without root nodes, naming `models` / `includeRootNodes`. **D**: `finding-things.md:28` — omit `includeDependencies` for orientation. |
| D77 | `mps-aspect-behavior/SKILL.md:35` | Documents the `this.*` rewrite for parsed behavior bodies but not that a local or foreach variable typed with a concept name (`for (Step s : this.steps)`) comes back as an unresolved `ClassifierType`, nor that `.size()` on a child list stays a (valid) Java call. | round 16: `S6-sonnet-1:72-78` (7 steps to replace the type with `SNodeType`); `S6-opus-1:54-57` (4 optional idiom swaps) | open — **D**: one sentence (text in `review.md` §F7). **S** (later): parser warning for an unresolved `ClassifierType` whose name matches a concept of the context language. |
| D78 | `mps_mcp_check_root_node_problems` | Identical messages are not collapsed: 27× "Language … can't be loaded" on one sandbox root is a 36 KB result. | round 16: `S8-sonnet-1:36-37` | open — **S**: `{"message":…,"count":27,"sample":<ref>}` for repeats. |
| D79 | `mps_mcp_list_open_projects`; `AGENTS_template.md:113` | The mandated skill-version-stamp comparison costs one Bash `cat` per run (9 of 12 runs). The server already reports `agentConfigRoot`. | round 16: e.g. `S5-sonnet-1:5`, `S6-sonnet-1:4`, `S7-sonnet-1:11` | open — **S**: `skillStamps` per project entry, and the template compares that. |
| A2 | study harness — `scripts/analyze_runs.py` | Chain counts treat a parallel batch as sequential pairs (S5-sonnet's 5 `print→print` pairs are one turn), and `rereads_after_compaction` counts only skill files, so it reports 0 where S8-sonnet re-read 127 KB of temp files and re-ran 9 `mps_dump` calls. | round 16: `runs-r17/analysis/review.md` "Analyzer notes" | open — weight chains by distinct message id; count temp-file / `mps_dump` re-reads after a compaction. |

D39 and everything before it were archived on 2026-09-22; D43–D47 on 2026-09-24; D49, D51, D52, D54, D55, D56, D57, D58, D59, D60, D61, D62 (`b727f965586a`), and D69 (`edbc0a14a9ec`) on 2026-09-25.
D48–D56 were filed on 2026-09-24 from `HOTSPOT_REPORT_round10.md` and `HOTSPOT_REPORT_round11.md`. Those rounds were measurement only; no remedy was chosen there. D49, D51, D52, D54, and D55 have since been fixed and archived. D56 was closed and archived on 2026-09-25: every "repeated miss" it cited was one parallel batch, recovered in one turn (`../docs/d56-near-miss-validation.md`). D58–D60 were filed on 2026-09-25 from `HOTSPOT_REPORT_round13.md`, when their remedies (R-e, R-f, R-g) were chosen at Gate 2. D58, D59, and D60 were fixed (`80d98c96cd89`) and archived the same day; D59 after its integration test passed. D61 was filed on 2026-09-25 from `HOTSPOT_REPORT_round14.md` and, like the D54 follow-up, fixed (remedies R-b and R-a) and archived the same day. D62–D69 were filed on 2026-09-25 from `HOTSPOT_REPORT_round15.md`; D62 was fixed (`b727f965586a`) and archived the same day, and D69 separately (`edbc0a14a9ec`), after its suite run and a live check.

Round 12 (`HOTSPOT_REPORT_round12.md`, 2026-09-25, S1+S3 × opus+sonnet, same `skillsSha256 78064c75…` / `inventorySha256 e6b87321…` as rounds 10–11) adds evidence to entries that were open at the time, without changing their status then: **D50**: `S1-sonnet-1:36–:48` 13 reference reads for one constraints root, `:59–:60` reads of `computed-sequences-*.md` under the wrong sibling folder `scope-fqn-reference/`, `:62–:64` re-reads of `:46–:48` (opus: 2 `cat` calls); **D51** (archived 2026-09-25): a second creator kind, `AnonymousClassCreator` placed as a bare expression (`S1-sonnet-1:73`), found the `GenericNewExpression.creator` wrapper only at `:86` after a 6-call search, although `computed-sequences-blueprint.md` (read at `:47`, `:63`) shows the wrapped form; **D56**: singular `conceptRef` ×3 in one parallel batch (`S1-sonnet-1:65–:67`→`:68`). Two one-off near-misses, not filed: `alter_nodes MAKE` `moduleReference` (`:90`→`:91`) and plural `nodeReferences` array on `check_root_node_problems` (`:92`→`:93`); both error messages named the fix. D3 did not reproduce again (`/tmp/*.json` accepted at `S1-sonnet-1:89, :107, :123`).

Round 13 (`HOTSPOT_REPORT_round13.md`, 2026-09-25, S1+S3 × opus+sonnet, `skillsSha256 7704a175…` / `inventorySha256 328db937…`, measured HEAD `80b2681a649c`; D55's `5e8c0db3edd4` came later and is **not** measured) adds evidence without changing any status:
- **D50** reproduces unchanged, with no D50 edit since round 12: `S1-sonnet-1:33–:70` has 26 skill reads or greps in the constraints phase. That includes four `grep -rn` under `mps-aspect-constraints/references/` (`:36, :44, :48, :50`), one catalog-wide `grep -rn "ancestor<"` over `.claude/skills/` (`:41`, 10.5 KB result), and four whole-file re-reads (`:63, :65, :69, :70` of `:33, :35, :34, :40`) just before writing each blueprint. Opus did the same aspect with 5 Bash reads (`S1-opus-1:45–:46, :51–:52, :54`). Across the round, skill reads were 68 / 370 KB, against 60 / 276 KB in round 12 and 39 / 332 KB at baseline. The analysis and plan for R-a are in `plugins/mcp-tools/docs/d50-skill-navigation-plan.md`.
- **D56**: no instance (`concept`, `conceptName` and `node` did not appear).
- **Archived D49 and D51 were not exercised**. No call omitted several required keys. Sonnet built the self-excluding scope without an anonymous class (`S1-sonnet-1:75`), so no creator had to be wrapped. Neither fix can be credited yet.
- **Round 12's two one-off near-misses did not repeat** (`alter_nodes MAKE` `moduleReference`, plural `nodeReferences` on `check_root_node_problems`), so they stay unfiled.
- **One new one-off near-miss, not filed**: `GET_ENUMERATION_LITERALS` `enumeration` instead of `enumerationRef` (`S1-sonnet-1:122–:123`, one parallel batch, then `:124–:125`). The message named the fix, and it cost 1 turn.

Round 14 (`HOTSPOT_REPORT_round14.md`, 2026-09-25, S9 × opus+sonnet, `skillsSha256 2fc7c9e0…` / `inventorySha256 e97199ab…`, HEAD `27fe9b9f7f0a` **plus the uncommitted D54 edits**, S9 `promptSha256 5129d9ef…`) filed D61 and adds evidence (D54 and D61 were archived 2026-09-25, after remedies R-a and R-b):
- **D54 re-measure, half met.** Both cells pass check 2, and each `kind:response` carries a `preview` (`3 nodes`, `[Flour, Milk, Egg]`) that matches `print_node(PLAIN TEXT)` apart from padding. The "without a `print_node` detour" half fails: both workers still printed both responses after reading the previews (`S9-opus-1:28–29`, `S9-sonnet-1:36–37`), each to get "the exact (full) text". `mps-console/SKILL.md:46` and the tool description both say "for the exact full text, call `print_node`", and neither says when the preview already is the full text. Candidate: **D** (a preview under 200 chars with no cut is the printed text), optional **S** (`previewComplete` flag). Saves 2 calls per run.
- **D50**: the same catalog-wide grep shape, reached from `mps-console` through `mcp-insertion.md:97` (sonnet: 13 skill reads / 54 KB; opus: 7 grep steps). D61 is the S9-local part of that navigation.
- D3 did not reproduce: `/tmp/*.json` was accepted as the `json` file path on both runs.

Not opened from those rounds:

- D34 is confirmed fixed (round 11 §3.6: S6 generated correctly packaged Java with zero package-related errors). Already in the archive.
- D3 did not reproduce (round 11 §3.8: `S6-sonnet-1:110`→`:111` wrote `/tmp/describe2_blueprint.json` and `update_node` read it back with no temp-directory rejection; the only error there was D52). One data point. The archive still says fixed 2026-09-15, while round 6 recorded the rejection open in practice. Reconcile those two records before reopening D3.
- `alter_nodes MAKE` with top-level `targets` / `rebuild` instead of nested `parameters` is accepted D29 (kept strict on purpose). Unchanged 1-retry cost: round 11 `S6-sonnet-1:117`→`:118`.
- `search_concepts` `query` still costs a retry even though the error already names `searchTexts` (round 10 `S2-sonnet-1:121`–`:123`, two misses; round 11 `S6-sonnet-1:99`→`:100`, one). That is the model ignoring a correct message, not a new docs defect.
- Hotspots that stayed fixed (#1 temp-file envelope, #2 per-root re-validation, #5 bulk-insert summary, and D34) and harness-level ToolSearch fetches (hotspot #8) are not defects to handle here.

The three harness defects found in round 8 (D40 user-skill guard, D41 pre-dispatch
misclassification, D42 no scripted way to enable the call log) were **fixed in the same commit** and
are recorded in `HOTSPOT_REPORT_round8.md` §6 and study lessons 32–34, not here.

Round 15 (`HOTSPOT_REPORT_round15.md`, 2026-09-25, S1+S2+S3 × opus+sonnet, `skillsSha256 b1706dd1…` / `inventorySha256 f865cb89…`, HEAD `b79e2ac999e7`, evidence `runs-r16`) filed D62–D69 and adds evidence without changing any other status:
- **D50** is now the family that decides the sonnet greenfield turn count. S1-sonnet made 49 skill-file accesses for 150 KB (baseline 9 / 160 KB; round 13 36 / 159 KB) while its MCP calls fell 71 → 66. The growth is many small section reads: index hop `S1-sonnet-1:86 → :87-89`; typesystem 9 accesses and behavior 12, against 3 and 4 in round 13. All 4 whole-file re-reads (`:63-65, :68`) follow the first compaction (step 61, constraints phase), as in every sonnet S1/S2 run since the baseline. Deep JSON prints upstream (`:31-34`, 49 KB; `S2-sonnet-1:53-54`, 86 KB) feed that compaction.
- **Archived D51/D52: first live instance.** At `S2-sonnet-1:101` the assignability hint named `GenericNewExpression.creator` with the replacement JSON. It was applied verbatim, and `:102` succeeded: 1-call recovery (`assignability_recovery_calls` 0, `unrecovered` 0).
- **Archived D56**: 9 alias or literal rejects across 3 runs (`search_concepts query` ×2, `get_concept_details conceptReference`, `print_node "text"` / `"PLAIN_TEXT"`, top-level `code` on `parse_java_and_insert`, a `properties` object map). 8 of 9 recover on the next call, and the error text names the fix every time. That is consistent with D56's closure, so none are refiled.
- D3 did not reproduce: `/tmp/*.json` was accepted (`S3-opus-1:11-12`).

Round 16 (`HOTSPOT_REPORT_round16.md`, 2026-09-25, S5–S10 × opus+sonnet, `skillsSha256 f6b20bbf…` / `inventorySha256 a7bc6420…`, measured HEAD `f576e7d5fad5`, all 12 cells PASS) filed D70–D79 and A2. It also adds evidence to open entries:
- **D53** reproduces: both S7 workers created 4 per-root JUnit configurations. `S7-opus-1` tried the model reference once and got the misleading `NOT_FOUND`.
- **Archived D58** holds for the kinds it covers. The `roots`-on-a-project-dump case is the new residual D76.
- **Archived D61** pays: S9 `Bash→Bash` went from 10 to 1 (sonnet) and from 7 to 0 (opus), and turns went from 44 to 25 and from 35 to 26.
- **D67** gains a second symptom. Sonnet escalates `shape`→`full` for `superConcept` as well as for `sourceNode` (D75).
