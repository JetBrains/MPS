You are working in the current folder, which contains a JetBrains MPS project you have not seen
before. MPS is running with the MCP server enabled and this project open. Follow CLAUDE.md and the
MPS skills.

Task: onboard yourself to this project and record what you learn for future agents.

1. Explore the project: modules, languages, concepts and their properties/children/references,
   editors, constraints, typesystem and generator, and the sample models.
2. Using the `mps-dsl-memory` skill, create the project-local skill
   `.agents/skills/statechart-dsl/SKILL.md` (with references as the skill prescribes) so a future
   agent can author models in this language without re-exploring it: concept reference, sample
   node references, JSON blueprint examples, and gotchas you discovered.
3. Done when the skill files exist and you have printed their paths and a 5-line summary of the
   language.

Do not modify any MPS model. Do not edit .mps/.mpl files as text. Stop and explain if blocked.
