Harness check only. Call the MPS MCP tool `mps_mcp_list_open_projects` once, with `projectPath`
set to the current working directory, then print the project names it returned and stop. Do not
read any files and do not modify anything.
