package jetbrains.mps.lang.test.matcher;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SNode;

public abstract class DifferenceItem {
  public DifferenceItem() {
  }

  protected void print(StringBuilder buf, int indent) {
    for (int i = 0; i < indent; i++) {
      buf.append(' ').append(' ');
    }
    buf.append(toString());
    buf.append('\n');
  }
  public static String describe(SNode node) {
    if (node == null) {
      return "<no target>";
    }
    return node.getPresentation() + " : " + node.getConcept().getName() + " [" + node.getNodeId() + "]";
  }
}
