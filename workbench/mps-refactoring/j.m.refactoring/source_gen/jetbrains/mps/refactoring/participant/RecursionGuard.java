package jetbrains.mps.refactoring.participant;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import java.util.List;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.LinkedList;

@GeneratedClass(nodeId = "3545777672627658600", model = "r:27bc780b-59b2-4d26-9db5-a38b63c35884(jetbrains.mps.refactoring.participant)")
/*package*/ final class RecursionGuard {
  private static final String KEY = "mps:recursionGuard";

  private final List<RefactoringParticipant.ParticipantApplied<?, ?>> myCallStack = ListSequence.fromList(new LinkedList<>());

  /*package*/ static RecursionGuard get(RefactoringSession session) {
    Object instance = session.getObject(KEY);
    if (instance instanceof RecursionGuard) {
      return (RecursionGuard) instance;
    }
    RecursionGuard rv = new RecursionGuard();
    session.putObject(KEY, rv);
    return rv;
  }

  private RecursionGuard() {
  }

  /*package*/ void enter(final RefactoringParticipant.ParticipantApplied<?, ?> pa) {
    if (ListSequence.fromList(myCallStack).any((it) -> pa.isSame(it))) {
      // todo: checked exception
      throw new IllegalStateException("infinite recursion detected");
    }
    ListSequence.fromList(myCallStack).addElement(pa);
  }
  /*package*/ void leave(RefactoringParticipant.ParticipantApplied<?, ?> pa) {
    RefactoringParticipant.ParticipantApplied<?, ?> removed = ListSequence.fromList(myCallStack).removeLastElement();
    assert removed == pa : "Corrupted recursion guard stack";
  }
}
